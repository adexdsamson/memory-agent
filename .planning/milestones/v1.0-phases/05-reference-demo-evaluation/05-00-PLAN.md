---
phase: "05"
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/mnema/adapters/vector_store/sqlite_t1.py
  - src/mnema/demo/__init__.py
  - src/mnema/demo/coach.py
  - src/mnema/eval/__init__.py
  - src/mnema/eval/baseline.py
  - tests/test_demo_coach.py
  - tests/test_eval_baseline.py
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
  - DEMO-03
  - DEMO-04
  - DEMO-05
  - EVAL-02

must_haves:
  truths:
    - "SqliteT1 has a public async close() method that flushes the WAL and closes the aiosqlite connection"
    - "src/mnema/demo/ and src/mnema/eval/ are importable Python packages (have __init__.py)"
    - "tests/test_demo_coach.py exists with six named test stubs (RED — all xfail or skip)"
    - "tests/test_eval_baseline.py exists with one named test stub (RED — xfail or skip)"
    - "uv run --extra dev pytest tests/test_demo_coach.py tests/test_eval_baseline.py collects 7 tests without ImportError"
  artifacts:
    - path: "src/mnema/adapters/vector_store/sqlite_t1.py"
      provides: "SqliteT1.close() public method"
      contains: "async def close"
    - path: "src/mnema/demo/__init__.py"
      provides: "demo package marker"
    - path: "src/mnema/demo/coach.py"
      provides: "CLI entrypoint skeleton (__main__ block + build_engine wiring)"
    - path: "src/mnema/eval/__init__.py"
      provides: "eval package marker"
    - path: "src/mnema/eval/baseline.py"
      provides: "eval module skeleton (Probe dataclass, containment_check stub)"
    - path: "tests/test_demo_coach.py"
      provides: "RED stubs: test_coach_entrypoint, test_cross_session_recall, test_supersession_surfaces_fields, test_decay_protected_and_recovery, test_budget_packing_and_expand"
    - path: "tests/test_eval_baseline.py"
      provides: "RED stub: test_eval_baseline_comparison"
  key_links:
    - from: "SqliteT1.close()"
      to: "self._db.close()"
      via: "await self._db.close() + PRAGMA wal_checkpoint(FULL) before close"
      pattern: "async def close"
    - from: "tests/test_demo_coach.py"
      to: "mnema.config.build_engine"
      via: "import at top of test file"
      pattern: "from mnema.config import"
---

<objective>
Wave 0 infrastructure: close the SqliteT1 gap identified in RESEARCH.md, create the demo and eval package scaffolding, and plant RED test stubs for all six Phase 5 requirements. Nothing goes GREEN here — tests are xfail stubs. This wave unblocks Wave 1.

Purpose: The DEMO-02 cross-session fixture requires SqliteT1.close() to flush the WAL before reopening the same file path in a second engine. Without it, internal access to engine._t1._db is required in every test. The RED stubs give Wave 1 a concrete test file to drive against.

Output: SqliteT1.close(), demo/__init__.py + coach.py skeleton, eval/__init__.py + baseline.py skeleton, test_demo_coach.py (5 RED stubs), test_eval_baseline.py (1 RED stub).
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-reference-demo-evaluation/05-CONTEXT.md
@.planning/phases/05-reference-demo-evaluation/05-RESEARCH.md
@.planning/phases/05-reference-demo-evaluation/05-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/mnema/adapters/vector_store/sqlite_t1.py:
  class SqliteT1 — has self._db (aiosqlite.Connection), self._dim (int)
  classmethod open(db_path: str, dim: int) -> SqliteT1
  async def upsert(record: MemoryRecord) -> None
  async def update(record_id: str, **fields: object) -> None  # _ALLOWED_COLUMNS whitelist
  async def upsert_with_vector(record: MemoryRecord, embedding: list[float]) -> None
  async def get_live_records(user_id: str) -> list[MemoryRecord]
  NO close() method exists yet — this is the gap to fix

From src/mnema/config.py:
  class LocalConfig(BaseModel):
    sqlite_path: str = ":memory:"
    local_fs_path: str = Field(default_factory=_tmpdir)
    vault_path: str = Field(default_factory=_tmpdir)
    embedder_dim: int = 128
  async def build_engine(config: MnemaConfig) -> MemoryEngine

From src/mnema/core/engine.py:
  class MemoryEngine — five verbs + scope()
  class ScopedHandle — remember(content, *, session_id), recall(query, *, budget), expand(record_id)
  engine._t1 — the SqliteT1 instance (private attribute)
  engine._scheduler — the InProcessScheduler instance
  engine.consolidate(*, user_id=None) -> None
  engine.evict(*, user_id: str) -> int

From tests/conftest.py (fixture style used across the suite):
  @pytest.fixture async def engine(tmp_path, stub_embedder) — yields MemoryEngine with ":memory:"
  asyncio_mode = "auto" is set in pyproject.toml
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SqliteT1.close() public method</name>
  <files>src/mnema/adapters/vector_store/sqlite_t1.py</files>
  <read_first>src/mnema/adapters/vector_store/sqlite_t1.py (full file — locate the open() classmethod and any existing cleanup, then insert close() in the appropriate place near open())</read_first>
  <behavior>
    - close() executes PRAGMA wal_checkpoint(FULL) then awaits self._db.close()
    - close() is idempotent: calling it twice does not raise (aiosqlite close on already-closed connection is a no-op in most versions; wrap in try/except if needed)
    - close() is async, returns None
  </behavior>
  <action>
    Locate the SqliteT1 class in sqlite_t1.py. Find the open() classmethod. Immediately after the open() classmethod (or grouped near it as the "lifecycle" block), add:

      async def close(self) -> None:
          """Flush the WAL and close the aiosqlite connection.

          Runs PRAGMA wal_checkpoint(FULL) before closing so that any in-flight
          WAL pages are merged into the main database file. This is required for
          DEMO-02 cross-session: closing engine 1's connection before opening
          engine 2 over the same file path guarantees engine 2 sees all writes.
          """
          await self._db.execute("PRAGMA wal_checkpoint(FULL)")
          await self._db.close()

    No other changes to sqlite_t1.py. Do not modify _ALLOWED_COLUMNS, DDL, or any other method.
  </action>
  <verify>
    <automated>uv run --extra dev pytest tests/ -q -x --tb=short -k "not test_demo_coach and not test_eval_baseline" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async def close" src/mnema/adapters/vector_store/sqlite_t1.py` returns at least one match
    - Existing test suite (`uv run --extra dev pytest tests/ -q -x --ignore=tests/test_demo_coach.py --ignore=tests/test_eval_baseline.py`) still exits 0 (no regression)
    - `grep -c "wal_checkpoint" src/mnema/adapters/vector_store/sqlite_t1.py` returns at least 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create demo and eval package scaffolding</name>
  <files>
    src/mnema/demo/__init__.py,
    src/mnema/demo/coach.py,
    src/mnema/eval/__init__.py,
    src/mnema/eval/baseline.py
  </files>
  <read_first>
    src/mnema/config.py (LocalConfig fields + build_engine signature),
    src/mnema/core/engine.py (ScopedHandle API),
    src/mnema/core/packer.py (TiktokenCounter class signature)
  </read_first>
  <action>
    Create four files:

    1. src/mnema/demo/__init__.py — empty (package marker only, no exports needed at this stage).

    2. src/mnema/demo/coach.py — skeleton with:
       - Module docstring: "MNEMA nutrition-coach reference demo (DEMO-01). CLI interactive chat loop on build_engine(LocalConfig). Per D5-01/D5-02/D5-04."
       - Top-level imports: asyncio, pathlib.Path, argparse, mnema.config (LocalConfig, build_engine)
       - A CoachSession dataclass or simple class with: engine, scope (ScopedHandle), data_dir (Path), session_id (str)
       - async def run_session(data_dir: Path, session_id: str) -> None — builds the engine via build_engine(LocalConfig(sqlite_path=..., local_fs_path=..., vault_path=...)), creates a ScopedHandle, runs a minimal chat loop that: (a) reads input from stdin, (b) calls scope.remember(turn, session_id=session_id), (c) calls scope.recall(turn, budget=500) and prints the returned records, (d) after the loop calls engine.consolidate() and engine._t1.close() (use the new method). Exits on "quit" or "exit" input.
       - async def suggest_meal(scope, query: str) -> str — calls scope.recall(query, budget=300), formats recalled facts as a constraint-respecting suggestion string (e.g., "Suggested meal considering your constraints: [list of constraints from results]").
       - if __name__ == "__main__" block that calls asyncio.run(run_session(...)) with args parsed from argparse (--data-dir defaulting to a fixed local path like "mnema_demo_data", --session-id defaulting to "session-1").

    3. src/mnema/eval/__init__.py — empty package marker.

    4. src/mnema/eval/baseline.py — skeleton with:
       - Module docstring referencing EVAL-02, D5-05..D5-08.
       - from dataclasses import dataclass; from pathlib import Path; from typing import Optional
       - @dataclass class Probe: query: str, must_contain: list[str], must_not_contain: list[str], req_id: str
       - def containment_check(context: str, must_contain: list[str], must_not_contain: list[str]) -> bool — checks all must_contain phrases appear case-insensitively AND all must_not_contain phrases are absent.
       - EVAL_BUDGET: int = 300 constant
       - A stub PROBES: list[Probe] = [] (will be populated in Wave 3)
       - async def run_eval(data_dir: Path) -> dict — stub that raises NotImplementedError("implement in Wave 3")
       - async def write_eval_report(results: dict, output_path: Path) -> None — stub that raises NotImplementedError

    All four files must be pyright-clean (strict): add type annotations everywhere. No fenced code in action — the above is directive prose describing exact signatures.
  </action>
  <verify>
    <automated>uv run --extra dev python -c "from mnema.demo.coach import run_session, suggest_meal; from mnema.eval.baseline import Probe, containment_check, EVAL_BUDGET; print('imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The python -c import check above exits 0 and prints "imports OK"
    - `uv run --extra dev pyright src/mnema/demo/ src/mnema/eval/` exits 0 (no type errors)
    - coach.py contains "if __name__" block
    - baseline.py contains class Probe and def containment_check
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Plant RED test stubs for DEMO-01..05 and EVAL-02</name>
  <files>tests/test_demo_coach.py, tests/test_eval_baseline.py</files>
  <read_first>
    tests/conftest.py (fixture patterns and asyncio_mode),
    tests/test_consolidation.py lines 1-60 (module-level helpers + fixture usage pattern),
    .planning/phases/05-reference-demo-evaluation/05-VALIDATION.md (per-req test map)
  </read_first>
  <action>
    Create tests/test_demo_coach.py with:
    - Module docstring citing DEMO-01..05
    - Import pytest, pathlib.Path, mnema.config (LocalConfig, build_engine) — deferred inside test bodies to allow RED collection even before implementation
    - Fixtures at module level:
        @pytest.fixture async def persistent_engine_factory(tmp_path) — yields a (make_engine, close_engine) tuple where make_engine() returns await build_engine(LocalConfig(sqlite_path=str(tmp_path/"mnema.db"), local_fs_path=str(tmp_path/"t0"), vault_path=str(tmp_path/"vault"))) and close_engine(eng) runs PRAGMA wal_checkpoint(FULL) via eng._t1.close() then shuts down the scheduler via eng._scheduler.shutdown().
    - Five async test functions, each marked @pytest.mark.xfail(strict=False, reason="RED stub — implement in Wave 1/2"):
        async def test_coach_entrypoint(tmp_path) — DEMO-01: imports coach module, calls suggest_meal on a freshly built engine, asserts result is a str
        async def test_cross_session_recall(persistent_engine_factory) — DEMO-02: opens engine1, remembers a peanut allergy, consolidates, closes; opens engine2 over same paths, recalls "food allergies", asserts "peanut" in some result content
        async def test_supersession_surfaces_fields(persistent_engine_factory) — DEMO-03: remembers "spicy food preference item 0" twice with consolidation in between, asserts old record valid_until is not None and superseded_by is not None
        async def test_decay_protected_and_recovery(persistent_engine_factory) — DEMO-04: seeds a peanut allergy + a kale transient (remembered then backdated via t1.update), runs evict, asserts: evicted_count >= 1, peanut still recalled, kale not in live records, expand(kale_id) returns non-None turn with "kale" in content
        async def test_budget_packing_and_expand(persistent_engine_factory) — DEMO-05: seeds 20+ records, recalls with budget=300, asserts non-protected token count fits budget, asserts expand() on a seeded record returns turn with original content

    Create tests/test_eval_baseline.py with:
    - Module docstring citing EVAL-02
    - One async test function marked @pytest.mark.xfail(strict=False, reason="RED stub — implement in Wave 3"):
        async def test_eval_baseline_comparison(tmp_path) — sets up seeded data, runs baseline.run_eval(tmp_path), asserts returned dict has keys "probes_passed_mnema", "probes_passed_naive", "token_reduction_pct"

    Both files must be importable and collectable by pytest even with the xfail stubs — use deferred imports inside test bodies (not at module top level) for engine internals.
  </action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_demo_coach.py tests/test_eval_baseline.py --collect-only -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - pytest --collect-only shows 6 test items collected (5 in test_demo_coach.py, 1 in test_eval_baseline.py) with no CollectError
    - `uv run --extra dev pytest tests/test_demo_coach.py tests/test_eval_baseline.py -q` exits 0 (all xfail, none ERROR)
    - No ImportError at collection time (module-top imports are only mnema.config and pytest — engine internals are deferred into test bodies)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test isolation → shared filesystem | Each test must use a separate tmp_path to prevent data leakage across tests |
| coach.py stdin → engine.remember() | User-typed content enters the engine; validated only by the engine's existing scope checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-00-01 | Tampering | SqliteT1.close() WAL checkpoint | mitigate | Explicit PRAGMA wal_checkpoint(FULL) before db.close() ensures no partial writes survive to engine 2 |
| T-05-00-02 | Information Disclosure | test persistent_engine_factory fixture | mitigate | Always use pytest tmp_path (unique per test) — never a shared global temp dir; prevents cross-test data leakage |
| T-05-00-03 | Denial of Service | InProcessScheduler not shut down in test teardown | accept | xfail stubs do not run the engine; Wave 1 close_engine() helper always calls scheduler.shutdown() |
| T-05-00-04 | Tampering | coach.py stdin input passed to engine.remember() | accept | Phase 5 is a local CLI demo with no network surface; input enters only the engine's existing T0 append path which is already scoped by user_id |
</threat_model>

<verification>
After Wave 0 completes:

```
uv run --extra dev pytest tests/test_demo_coach.py tests/test_eval_baseline.py -q
```
Must exit 0. All 6 tests are xfail (not ERROR, not FAILED).

```
uv run --extra dev pytest tests/ -q -x --ignore=tests/test_demo_coach.py --ignore=tests/test_eval_baseline.py
```
Must exit 0. Existing test suite has no regression from the SqliteT1.close() addition.

```
uv run --extra dev pyright src/mnema/demo/ src/mnema/eval/
```
Must exit 0.
</verification>

<success_criteria>
- SqliteT1.close() exists and performs WAL checkpoint before close
- src/mnema/demo/ and src/mnema/eval/ are importable packages with skeleton implementations
- 6 RED test stubs collected by pytest without ImportError
- All xfail (exit 0), no regressions in the existing suite
</success_criteria>

<output>
After completion, create .planning/phases/05-reference-demo-evaluation/05-00-SUMMARY.md
</output>
