---
phase: "05"
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - tests/test_demo_coach.py
autonomous: true
requirements:
  - DEMO-04
  - DEMO-05

must_haves:
  truths:
    - "test_decay_protected_and_recovery passes: backdated kale transient is evicted; pinned peanut allergy survives; expand() on the evicted record's id returns a Turn with 'kale' in content"
    - "test_budget_packing_and_expand passes: non-protected records fit within the token budget; the protected allergy record appears in results regardless of budget; expand() on a seeded record returns the verbatim Turn"
    - "The keep_score math for a 60-day-old record with salience=0.2 and access_count=0 is verified to be < KEEP_THRESHOLD=0.3"
  artifacts:
    - path: "tests/test_demo_coach.py"
      provides: "test_decay_protected_and_recovery and test_budget_packing_and_expand GREEN"
  key_links:
    - from: "test_decay_protected_and_recovery"
      to: "engine.t1.update(record_id, created_at=past, last_accessed=past, salience=0.2)"
      via: "backdating via _ALLOWED_COLUMNS whitelist"
      pattern: "t1.update"
    - from: "test_decay_protected_and_recovery"
      to: "engine.evict(user_id=...)"
      via: "batch decay pass returning eviction count"
      pattern: "evict"
    - from: "test_budget_packing_and_expand"
      to: "TiktokenCounter().count(r.summary or r.content[:80])"
      via: "token-budget assertion over non-protected results"
      pattern: "TiktokenCounter"
---

<objective>
Wave 2 — implement and GREEN DEMO-04 (decay + protected fact + cold-store recovery) and DEMO-05 (budget packing + verbatim expand). These scenarios prove the remaining two Core Value pillars: protected facts provably survive every decay pass, and recall fits within a token budget while always surfacing critical facts.

Purpose: DEMO-04 demonstrates the keep_score math produces the right eviction decision, the protected flag is structurally enforced (not scored), and the cold-store recovery path via expand() works. DEMO-05 demonstrates the two-pass packer's budget guarantee under load.

Output: Two additional GREEN tests in test_demo_coach.py (DEMO-04 and DEMO-05).
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/05-reference-demo-evaluation/05-CONTEXT.md
@.planning/phases/05-reference-demo-evaluation/05-RESEARCH.md
@.planning/phases/05-reference-demo-evaluation/05-01-SUMMARY.md

<interfaces>
<!-- Key contracts. No codebase exploration needed. -->

From src/mnema/core/engine.py:
  engine.evict(*, user_id: str) -> int   — returns count of evicted records
  engine.expand(record_id: str, *, user_id: str) -> Turn | None   — scope-checked T0 fetch
  engine.t1 (property) -> SqliteT1   — exposes the T1 adapter
  engine.t1.get_live_records(user_id: str) -> list[MemoryRecord]
  engine.t1.get(record_id: str) -> MemoryRecord | None   — returns retired records too
  engine.t1.update(record_id: str, **fields) -> None   — _ALLOWED_COLUMNS includes created_at, last_accessed, salience

From src/mnema/core/schema.py:
  MemoryRecord.id: str
  MemoryRecord.content: str
  MemoryRecord.protected: bool   — set by safety-keyword detection during consolidation
  MemoryRecord.valid_until: datetime | None   — None = live record
  MemoryRecord.t0_ref: str | None   — must be non-None for expand() to return a Turn

From src/mnema/core/packer.py:
  class TiktokenCounter:
    def __init__(self) -> None   — lazily imports tiktoken cl100k_base
    def count(self, text: str) -> int   — returns token count

From src/mnema/core/decay.py:
  KEEP_THRESHOLD = 0.3   (also exposed as engine.KEEP_THRESHOLD in engine.py)
  keep_score formula (pre-computed in RESEARCH.md):
    60-day-old record, salience=0.2, access_count=0:
    keep_score = 0.4 * exp(-0.05*60) + 0.3 * 0 + 0.3 * 0.2 = 0.0199 + 0 + 0.06 = 0.0799
    0.0799 < 0.3 → WILL be evicted

CRITICAL — DEMO-04 seeding pattern (per RESEARCH.md §DEMO-04 verified):
  1. await scope.remember("I used to enjoy kale smoothies", session_id="s1") → creates T0 turn + provisional T1; t0_ref is set
  2. await engine.consolidate() → provisional → confirmed T1 record; t0_ref preserved
  3. live = await engine.t1.get_live_records(user_id); kale_r = next(r for r in live if "kale" in r.content)
  4. past = datetime.now(UTC) - timedelta(days=60)
  5. await engine.t1.update(kale_r.id, created_at=past, last_accessed=past, salience=0.2)
  6. await engine.evict(user_id=user_id) → kale_r.id evicted, allergy stays
  7. await scope.expand(kale_r.id) → reads T0 JSONL via t0_ref → returns Turn

CRITICAL — DEMO-04 allergy seeding:
  The peanut allergy ("I am allergic to peanuts") contains "allerg" which triggers _is_safety_claim() in consolidation → protected=True. The protected flag means decay_pass skips it before keep_score math.

CRITICAL — DEMO-05 budget assertion (per RESEARCH.md §DEMO-05 + Pitfall 8):
  pack_records() always includes protected records even if budget is exceeded.
  CORRECT assertion: non_protected_tokens <= budget  (not total_tokens <= budget)
  Import TiktokenCounter from mnema.core.packer — already in pyproject.toml core deps.

CRITICAL — expand() returns None when t0_ref is None (Pitfall 5):
  Only records seeded via scope.remember() (not direct t1.upsert()) have a valid t0_ref.
  For DEMO-04, the kale record must be seeded via remember() + consolidate() first.
  For DEMO-05, expand() should be called on a record seeded via remember(); check result is not None before asserting content.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GREEN test_decay_protected_and_recovery (DEMO-04)</name>
  <files>tests/test_demo_coach.py</files>
  <read_first>tests/test_demo_coach.py (current state after Wave 1 — locate test_decay_protected_and_recovery stub)</read_first>
  <behavior>
    - Peanut allergy record: protected=True after consolidation (safety keyword detection); NOT evicted by evict()
    - Kale transient: backdated to 60 days ago with salience=0.2 via t1.update(); evicted by evict() (keep_score=0.08 < 0.3)
    - evict() return value >= 1
    - After evict(): recall("allergy") returns at least one record with "peanut" in content
    - After evict(): get_live_records() has no record with "kale" in content
    - expand(kale_id) returns a Turn (not None) with "kale" in content — the T0 turn was written before backdating
  </behavior>
  <action>
    Open tests/test_demo_coach.py. Remove the @pytest.mark.xfail marker from test_decay_protected_and_recovery and implement it:

    Imports needed (deferred inside test body): datetime, timedelta, timezone from datetime; engine internals via make_engine.

    Implementation:
    1. engine = await make_engine(); scope = engine.scope(user_id="demo_user")
    2. Seed allergy: await scope.remember("I am allergic to peanuts", session_id="s1"); await engine.consolidate()
    3. Seed kale transient: await scope.remember("I used to enjoy kale smoothies", session_id="s1"); await engine.consolidate()
    4. Find kale record: live = await engine.t1.get_live_records("demo_user"); kale_r = next(r for r in live if "kale" in r.content)
    5. Store kale_id = kale_r.id
    6. Backdate: past = datetime.now(timezone.utc) - timedelta(days=60); await engine.t1.update(kale_id, created_at=past, last_accessed=past, salience=0.2)
    7. Evict: evicted_count = await engine.evict(user_id="demo_user"); assert evicted_count >= 1
    8. Assert allergy survived: allergy_results = await scope.recall("allergy", budget=500); assert any("peanut" in r.content for r in allergy_results), "Protected allergy must survive eviction"
    9. Assert kale gone from live: live2 = await engine.t1.get_live_records("demo_user"); assert not any("kale" in r.content for r in live2), "Kale transient must be evicted from live records"
    10. Cold-store recovery: turn = await scope.expand(kale_id); assert turn is not None, "expand() must return the T0 turn for an evicted record with valid t0_ref"; assert "kale" in turn.content, "Verbatim T0 turn must contain original content"
    11. close_engine(engine)

    Add an inline comment block before step 7: "DEMO-04 proof: keep_score(60d, sal=0.2, acc=0) = 0.08 < KEEP_THRESHOLD=0.3 → evicted. Protected allergy skipped before score math (decay_pass structural guarantee, FORG-03)."
  </action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_demo_coach.py::test_decay_protected_and_recovery -xvs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --extra dev pytest tests/test_demo_coach.py::test_decay_protected_and_recovery -x` exits 0 with PASSED
    - Test asserts: evicted_count >= 1, "peanut" in allergy recall results, "kale" NOT in live records, turn is not None, "kale" in turn.content
    - No xfail marker on this test
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN test_budget_packing_and_expand (DEMO-05)</name>
  <files>tests/test_demo_coach.py</files>
  <read_first>tests/test_demo_coach.py (current state after Task 1)</read_first>
  <behavior>
    - 20 non-protected "meal fact" records seeded, plus 1 protected allergy record
    - recall(budget=BUDGET) with BUDGET=300: protected allergy appears in results regardless of budget
    - non_protected_tokens (sum of TiktokenCounter.count(r.summary or r.content[:80]) for non-protected results) <= BUDGET
    - expand() on a record that was seeded via remember() returns a Turn with content matching the original string
    - expand() on a record with no t0_ref returns None (guard assertion)
  </behavior>
  <action>
    Open tests/test_demo_coach.py. Remove the @pytest.mark.xfail marker from test_budget_packing_and_expand and implement it:

    BUDGET = 300 (local constant in test body, or imported from mnema.eval.baseline if defined there)

    Implementation:
    1. engine = await make_engine(); scope = engine.scope(user_id="demo_user")
    2. Seed allergy: await scope.remember("I am allergic to peanuts", session_id="s1")
    3. Seed 20 non-protected records: for i in range(20): await scope.remember(f"meal fact {i}: I enjoy various foods item {i}", session_id="s1")
    4. await engine.consolidate()
    5. Recall with budget: results = await scope.recall("meal history", budget=BUDGET)
    6. Import TiktokenCounter from mnema.core.packer (deferred import inside test body)
    7. counter = TiktokenCounter()
    8. protected_results = [r for r in results if r.protected]
    9. non_protected_results = [r for r in results if not r.protected]
    10. non_protected_tokens = sum(counter.count(r.summary or r.content[:80]) for r in non_protected_results)
    11. assert non_protected_tokens <= BUDGET, f"Non-protected tokens {non_protected_tokens} must fit within budget {BUDGET}"
    12. assert any(r.protected for r in results), "Protected allergy must always appear in budgeted recall (two-pass packer guarantee)"
    13. assert any("peanut" in r.content for r in protected_results), "Protected result must be the peanut allergy"
    14. Verbatim expand: pick any record from results that was seeded via remember() — use the allergy record (protected_results[0]). turn = await scope.expand(protected_results[0].id); assert turn is not None; assert "peanut" in turn.content
    15. close_engine(engine)

    Add inline comment before step 11: "DEMO-05 proof: two-pass packer (RECALL-05) reserves protected slots first — allergy guaranteed in results. Non-protected records packed within remaining budget."

    Note: the 20 meal fact records may not all make it into results (budget = 300 tokens is small). This is expected and correct — it demonstrates the budget packing working.
  </action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_demo_coach.py::test_budget_packing_and_expand -xvs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --extra dev pytest tests/test_demo_coach.py::test_budget_packing_and_expand -x` exits 0 with PASSED
    - Assertions in test: non_protected_tokens <= 300, protected allergy in results, turn is not None, "peanut" in turn.content
    - No xfail marker on this test
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Full demo suite GREEN + regression check</name>
  <files>tests/test_demo_coach.py</files>
  <read_first>tests/test_demo_coach.py (final state after Tasks 1 and 2)</read_first>
  <action>
    Run the full test_demo_coach.py file. All five tests (DEMO-01..05) should now be GREEN. If test_coach_entrypoint is still xfail, implement it: make_engine() → scope → scope.remember("I am allergic to peanuts", session_id="s1") → await suggest_meal(scope, "dinner") → assert isinstance(result, str).

    Also run the full test suite to ensure no regressions:
    - `uv run --extra dev pytest tests/ -q -x` must exit 0
    - `uv run --extra dev pyright src/mnema/` must exit 0 (no new type errors from the coach.py changes)

    If test_coach_entrypoint was already GREEN in Wave 1 (it should be from Task 1 of 05-01), leave it as-is. The goal of this task is to verify the complete state and fix any remaining xfail stubs.
  </action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_demo_coach.py -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --extra dev pytest tests/test_demo_coach.py -v` shows 5 PASSED, 0 FAILED, 0 xfail
    - `uv run --extra dev pytest tests/ -q -x` exits 0 (full suite, no regression)
    - `uv run --extra dev pyright src/mnema/ --ignore-external` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| t1.update() backdating | Caller can set arbitrary created_at/last_accessed — only allowed via _ALLOWED_COLUMNS whitelist (T-1-05) |
| evict() → archive() → expand() | Cold-store path: archived record must be read back via t0_ref; no cross-user access possible due to scope check in expand() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | t1.update() backdating in DEMO-04 | accept | Backdating is a test-only setup step using the documented _ALLOWED_COLUMNS whitelist; no production code bypasses; the update path was designed for this use case (RESEARCH.md §DEMO-04 verified) |
| T-05-02-02 | Information Disclosure | expand() on evicted record reads T0 JSONL | mitigate | expand() scope-checks record.user_id == user_id before fetching from T0; cross-user T0 access is impossible (engine.py lines 257-259) |
| T-05-02-03 | Elevation of Privilege | protected flag bypassed via direct t1.update() | mitigate | Demo tests never call t1.update(protected=False); the protected flag is only set during consolidation by safety-keyword detection; tests assert protection survives eviction, they do not attempt to clear it |
| T-05-02-04 | Denial of Service | 20 records seeded + consolidation in DEMO-05 may time out | accept | All operations are local sqlite/in-process; 20 records consolidate in <1s; no timeout risk in deterministic test environment |
</threat_model>

<verification>
After Wave 2 completes:

```
uv run --extra dev pytest tests/test_demo_coach.py -v
```
Must show: 5 PASSED, 0 FAILED, 0 xfail.

```
uv run --extra dev pytest tests/ -q -x
```
Must exit 0.
</verification>

<success_criteria>
- DEMO-04: backdated kale transient evicted; peanut allergy with protected=True survives; expand() returns verbatim T0 Turn
- DEMO-05: non-protected recall results fit within 300-token budget; protected allergy always included; expand() on a seeded record returns non-None Turn
- All 5 DEMO tests GREEN in test_demo_coach.py
- Full suite exits 0
</success_criteria>

<output>
After completion, create .planning/phases/05-reference-demo-evaluation/05-02-SUMMARY.md
</output>
