---
phase: 04-cloud-providers-backends
plan: "08"
type: execute
wave: 5
depends_on: ["04-07"]
files_modified: []
autonomous: true
requirements:
  - PROV-03
  - PROV-04
  - PROV-05
  - PROV-07
  - STORE-01
  - STORE-02
  - STORE-03
  - STORE-04
  - STORE-05
  - STORE-06
  - SCHED-03

must_haves:
  truths:
    - "uv run --extra dev pytest tests/ -q exits 0 (full hermetic suite GREEN including conformance local backends)"
    - "uv run --extra dev pyright exits 0 (0 errors, 0 warnings)"
    - "uv run --extra dev ruff check src/ tests/ exits 0 (0 violations)"
    - "Every requirement ID (PROV-03/04/05/07, STORE-01..06, SCHED-03) is covered by a passing or cleanly-skipping test"
    - "Conformance suite local-backend parametrizations: sqlite (t1), stub (embedder+llm), local_fs + moto_s3 (object_store), local_fs_vault (vault), in_process + cron (scheduler) — all PASS"
    - "No cloud test fails with ERROR (all cloud params skip cleanly without MNEMA_TEST_* env vars)"
  artifacts:
    - path: "tests/conformance/"
      provides: "Full conformance suite: 7 contract modules, all local backends passing"
    - path: "src/mnema/config.py"
      provides: "build_engine() factory verified end-to-end"
    - path: "src/mnema/migrate.py"
      provides: "reindex_all() verified"
    - path: ".planning/phases/04-cloud-providers-backends/04-08-SUMMARY.md"
      provides: "Phase gate summary"
  key_links:
    - from: "uv run --extra dev pytest tests/ -q"
      to: "exit code 0"
      via: "all hermetic tests pass, cloud params skip"
      pattern: "passed.*skipped"
    - from: "uv run --extra dev pyright"
      to: "0 errors"
      via: "strict type checking on src/mnema"
      pattern: "0 errors"
---

<objective>
Phase gate: verify the entire Phase 4 implementation is correct, complete, and clean. Run the three mandatory gate commands and fix any remaining issues. Document the phase result.

Purpose: The phase gate is the final check that all 11 requirements are covered, the hermetic suite is green, type checking passes, and no linting violations exist. No new code is written here — only fixes, documentation, and sign-off.
Output: Phase gate GREEN; 04-08-SUMMARY.md with coverage table and gate commands output.
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cloud-providers-backends/04-CONTEXT.md
@.planning/phases/04-cloud-providers-backends/04-07-SUMMARY.md
@.planning/phases/04-cloud-providers-backends/04-06-SUMMARY.md
@.planning/phases/04-cloud-providers-backends/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run gate commands and fix any issues</name>
  <files></files>
  <action>
    Run the three mandatory phase gate commands in order. For each failure, diagnose root cause and fix before proceeding to the next command.

    Command 1: uv run --extra dev pytest tests/ -q
    Expected: all tests pass or skip; no FAILED or ERROR.
    If failures exist:
      - FAILED hermetic test: fix the implementation (the failing test identifies the specific adapter/module)
      - ERROR in conformance import: fix the conftest or missing import
      - FAILED cloud test without env vars: the gate requirement is SKIP not PASS for cloud tests — fix the skipif guard
    Do not change test assertions to force-pass. Fix the underlying code.

    Command 2: uv run --extra dev pyright
    Expected: 0 errors, 0 warnings, 0 informations.
    If errors exist: fix type annotations in src/mnema/ files (the failing adapters/config/migrate).
    Common pyright issues in new adapters:
      - Missing return type annotation on async methods
      - Any type in Protocol-satisfying methods without # type: ignore
      - Missing from __future__ import annotations causing string annotation issues
      - asyncio.to_thread closure type inference — add explicit return type to the _call() inner function

    Command 3: uv run --extra dev ruff check src/ tests/
    Expected: All checks passed.
    If violations exist: run uv run --extra dev ruff check --fix src/ tests/ to auto-fix, then re-run to confirm.
    Do NOT use --unsafe-fixes. If a violation cannot be auto-fixed, fix manually.

    After all three pass, record the exact output of each command in the SUMMARY.

    Requirement coverage audit (in-task verification — not a separate command):
    For each requirement ID, verify the covering test passes:
      PROV-03: tests/conformance/test_llm_contract.py (stub param) + test_embedding_contract.py (stub param) — PASS; qwen params SKIP
      PROV-04: tests/conformance/test_llm_contract.py anthropic param — SKIP (gated)
      PROV-05: tests/conformance/test_embedding_contract.py voyage param — SKIP (gated); VoyageEmbedder imports OK
      PROV-07: tests/test_reindex.py — PASS
      STORE-01: tests/conformance/test_object_store_contract.py local_fs + moto_s3 — PASS
      STORE-02: tests/conformance/test_record_store_contract.py + test_vector_index_contract.py sqlite — PASS; postgres — SKIP
      STORE-03: LocalFSVault satisfies (already verified in Phase 3); conformance test_vault_contract.py local_fs_vault — PASS
      STORE-04: tests/test_factory.py — PASS
      STORE-05: tests/test_factory.py local end-to-end — PASS; qwen-alibaba — SKIP
      STORE-06: conformance suite runs ≥2 backends per axis hermetically (sqlite+postgres-skip, stub+voyage-skip, local_fs+moto_s3, local_fs_vault, in_process+cron)
      SCHED-03: tests/test_cron.py + tests/conformance/test_scheduler_contract.py cron — PASS
  </action>
  <verify>
    <automated>cd /c/Users/HomePC/Documents/GitHub/memoryAgent && uv run --extra dev pytest tests/ -q 2>&1 | tail -5 && uv run --extra dev pyright 2>&1 | tail -3 && uv run --extra dev ruff check src/ tests/ 2>&1 | tail -3</automated>
  </verify>
  <done>
    All three gate commands exit 0;
    pytest: all tests pass or skip (no FAILED, no ERROR);
    pyright: 0 errors;
    ruff: All checks passed
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md and create phase summary</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Update .planning/ROADMAP.md Phase 4 entry:
    - Update Plans count: "**Plans:** 9 plans"
    - Add the plan list with wave structure (all 9 plans, 04-00 through 04-08)
    - Do NOT change Phase 5 or other phases

    Create .planning/phases/04-cloud-providers-backends/04-08-SUMMARY.md using the template at $HOME/.claude/get-shit-done/templates/summary.md. Include:
    - Gate command outputs (exact text)
    - Requirements coverage table with test evidence for all 11 requirements
    - Adapters shipped: AnthropicLLM, QwenLLM, VoyageEmbedder, QwenEmbedder, PostgresT1, OSSS3Store, CronScheduler, MnemaConfig/build_engine, reindex_all
    - STORE-03 note: LocalFSVault already satisfied STORE-03 (shipped Phase 3); conformance-verified in Phase 4
    - Open questions resolved: moto[s3] added for hermetic S3 tests; voyageai.AsyncClient vs to_thread preference noted; PostgresT1 column-width check recommendation documented
    - Known limitations: DashScope call shapes assumed (A1, A7) — verified at cloud install time; OSS append is not atomic under concurrent writers (v2 enhancement)
    - What's next: Phase 5 (nutrition coach demo + evaluation)

    Also update .planning/STATE.md:
    - stopped_at: "Phase 04 complete (9/9) — ready to plan Phase 5"
    - completed_phases: 3 (update to reflect Phase 4 now complete after gate)
    - completed_plans: 24 (15 existing + 9 Phase 4 plans)
    - last_activity: today's date (2026-06-14)
  </action>
  <verify>
    <automated>cd /c/Users/HomePC/Documents/GitHub/memoryAgent && grep -c "04-08-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP.md updated with Phase 4 plan list; 04-08-SUMMARY.md created with gate output and coverage table; STATE.md updated to reflect Phase 4 complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase gate → CI | Gate commands run hermetically; no network required; no credentials exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Information Disclosure | Summary contains API key in example | mitigate | Summary docs use placeholder values (e.g., "ANTHROPIC_API_KEY=sk-...") never real keys; gate run uses no real credentials |
| T-04-08-02 | Tampering | Force-passing a failing test by weakening assertion | mitigate | Gate plan explicitly prohibits changing test assertions to force-pass; only implementation fixes are permitted |
</threat_model>

<verification>
Full phase gate:
1. `uv run --extra dev pytest tests/ -q` — exits 0
2. `uv run --extra dev pyright` — 0 errors
3. `uv run --extra dev ruff check src/ tests/` — All checks passed

Conformance local-backend coverage spot-check:
4. `uv run --extra dev pytest tests/conformance/ -q -v 2>&1 | grep -E "PASSED|SKIPPED" | grep "PASSED" | wc -l` — non-zero count of passing conformance tests
5. `uv run --extra dev pytest tests/conformance/ -q -v 2>&1 | grep "ERROR"` — no output (no collection or runtime errors)
</verification>

<success_criteria>
- pytest exits 0: all hermetic tests pass; cloud params skip; no ERROR
- pyright exits 0: strict type checking passes for all src/mnema/ files added in Phase 4
- ruff exits 0: no linting violations
- All 11 Phase 4 requirement IDs have verified test evidence in 04-08-SUMMARY.md
- ROADMAP.md Phase 4 updated with 9-plan wave structure
- STATE.md reflects Phase 4 complete
- Phase is ready for /gsd-verify-work and then Phase 5
</success_criteria>

<output>
After completion, create `.planning/phases/04-cloud-providers-backends/04-08-SUMMARY.md`
</output>
