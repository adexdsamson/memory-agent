---
phase: 04-cloud-providers-backends
plan: "03"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/mnema/adapters/embedding/voyage.py
  - src/mnema/adapters/embedding/qwen.py
autonomous: true
requirements:
  - PROV-03
  - PROV-05

must_haves:
  truths:
    - "VoyageEmbedder.embed() returns L2-normalized float vectors of length 1024 (per D4-06/D4-07)"
    - "QwenEmbedder.embed() returns L2-normalized float vectors of length 1024 (default dim)"
    - "Both adapters expose a dim property returning the configured output dimension"
    - "Both adapters satisfy EmbeddingProvider Protocol by structural typing — no Protocol inheritance"
    - "llm=anthropic, embedder=voyage is a valid independent-axis config (D4-06 explicit test)"
    - "Conformance embedder_backend voyage/qwen_embed params skip without env vars; pass when set"
  artifacts:
    - path: "src/mnema/adapters/embedding/voyage.py"
      provides: "VoyageEmbedder: Voyage voyage-3.5 embedding adapter with L2-normalization"
      exports: ["VoyageEmbedder"]
    - path: "src/mnema/adapters/embedding/qwen.py"
      provides: "QwenEmbedder: DashScope text-embedding-v4 adapter with L2-normalization"
      exports: ["QwenEmbedder"]
  key_links:
    - from: "VoyageEmbedder.embed()"
      to: "voyageai.Client.embed(texts, model=..., output_dimension=...).embeddings"
      via: "asyncio.to_thread(_call) + _l2_normalize()"
      pattern: "asyncio.to_thread"
    - from: "QwenEmbedder.embed()"
      to: "dashscope.TextEmbedding.call(model=..., input=texts, dimension=dim)"
      via: "asyncio.to_thread(_call) + _l2_normalize()"
      pattern: "asyncio.to_thread"
---

<objective>
Implement VoyageEmbedder and QwenEmbedder — the two real embedding adapters. Voyage is the canonical Claude-compatible embedder (PROV-05, D4-06); Qwen text-embedding-v4 is the default-stack embedder (PROV-03). Both normalize output to L2 unit vectors per D4-07 and EmbeddingProvider Protocol contract.

Purpose: PROV-05 (Claude+Voyage independent-axis config) and PROV-03 (Qwen embedder) are required for the documented default and the Claude+Voyage configs. The startup dim assertion in MemoryEngine.__init__ makes the dim property a first-class contract.
Output: src/mnema/adapters/embedding/voyage.py (VoyageEmbedder), src/mnema/adapters/embedding/qwen.py (QwenEmbedder)
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-cloud-providers-backends/04-CONTEXT.md
@.planning/phases/04-cloud-providers-backends/04-RESEARCH.md
@.planning/phases/04-cloud-providers-backends/04-PATTERNS.md
@.planning/phases/04-cloud-providers-backends/04-01-SUMMARY.md

<interfaces>
<!-- Analog: src/mnema/adapters/embedding/stub.py -->
Protocol contract (src/mnema/ports/embedding.py):
  class EmbeddingProvider(Protocol):
    @property
    def dim(self) -> int: ...
    async def embed(self, texts: list[str]) -> list[list[float]]: ...
  Normalization contract: embed() ALWAYS returns L2-normalized (unit) vectors.

StubEmbedder shape (analog):
  class StubEmbedder:
    def __init__(self, dim: int) -> None: self._dim = dim
    @property
    def dim(self) -> int: return self._dim
    async def embed(self, texts: list[str]) -> list[list[float]]: ...
      returns L2-normalized vectors of length self._dim

L2 normalize helper (from 04-PATTERNS.md embedding section):
  def _l2_normalize(v: list[float]) -> list[float]:
    norm = math.sqrt(sum(x * x for x in v)) or 1.0
    return [x / norm for x in v]

VoyageEmbedder SDK call (04-RESEARCH.md Pattern 3 + Pitfall 3):
  import voyageai
  client = voyageai.Client(api_key=api_key)   # sync; OR use voyageai.AsyncClient for native async
  result = client.embed(texts, model=self._model, output_dimension=self._output_dimension)
  raw_embeddings = result.embeddings  # list[list[float]]
  Pitfall 3: wrap sync Client.embed() in asyncio.to_thread; or use AsyncClient directly

QwenEmbedder SDK call (04-RESEARCH.md Pattern 4, ASSUMED — verify at install):
  from dashscope import TextEmbedding
  resp = TextEmbedding.call(model=TextEmbedding.Models.text_embedding_v4, input=texts, dimension=self._dim)
  raw = [item["embedding"] for item in resp.output["embeddings"]]
  NOTE (A7): model constant may be string "text-embedding-v4" if Models.text_embedding_v4 is unavailable

Independent-axis constraint (CLAUDE.md + D4-06):
  VoyageEmbedder(dim=1024) must work when paired with AnthropicLLM — the embedding axis is
  completely independent. Verify: VoyageEmbedder(api_key=..., output_dimension=1024).dim == 1024

Startup dim assertion wiring (engine.py line 98):
  if hasattr(t1, "_dim") and embedder.dim != t1._dim: raise ValueError(...)
  Both adapters must expose self._dim and dim property so this assertion fires correctly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VoyageEmbedder adapter (PROV-05)</name>
  <files>src/mnema/adapters/embedding/voyage.py</files>
  <behavior>
    - VoyageEmbedder(api_key="test", model="voyage-3.5", output_dimension=1024) — constructor does not raise
    - voyager.dim == 1024
    - embed(["hello"]) returns list of length 1; each vector has length 1024
    - L2-norm of each returned vector is within 0.01 of 1.0 (unit vector assertion)
    - Embedder satisfies EmbeddingProvider Protocol shape (pyright checks)
    - Independent axis: VoyageEmbedder can be combined with AnthropicLLM in the same engine config
  </behavior>
  <read_first>
    src/mnema/adapters/embedding/stub.py — copy class shape, dim property, __init__, L2 normalization
    04-RESEARCH.md Pattern 3 (lines ~318-354) — voyageai.Client.embed() call shape, output_dimension param
    04-PATTERNS.md VoyageEmbedder section (lines ~115-163) — full implementation pattern
    04-RESEARCH.md Pitfall 3 (lines ~739-745) — AsyncClient vs to_thread
    src/mnema/ports/embedding.py — Protocol contract
  </read_first>
  <action>
    Create src/mnema/adapters/embedding/voyage.py:

    Module docstring: "VoyageEmbedder — Voyage AI embedding adapter. Satisfies EmbeddingProvider Protocol by structural typing. Claude-compatible embedder (PROV-05, D4-06) — direct voyageai SDK, no LiteLLM (D4-05). L2-normalized at adapter (D4-07). Sync Client wrapped in asyncio.to_thread (D-13); switch to AsyncClient if event-loop contention occurs."

    Imports: from __future__ import annotations; import asyncio; import math; (voyageai imported lazily in __init__)

    Module-level helper: _l2_normalize(v: list[float]) -> list[float] — copy from stub.py math pattern: norm = math.sqrt(sum(x * x for x in v)) or 1.0; return [x / norm for x in v]

    Class VoyageEmbedder (no Protocol inheritance):
      __init__(self, api_key: str, model: str = "voyage-3.5", output_dimension: int = 1024) -> None:
        import voyageai  # noqa: PLC0415
        self._client = voyageai.Client(api_key=api_key)
        self._model = model
        self._output_dimension = output_dimension
        self._dim = output_dimension

      dim property -> int: return self._dim

      async embed(self, texts: list[str]) -> list[list[float]]:
        def _call() -> list[list[float]]:
          result = self._client.embed(texts, model=self._model, output_dimension=self._output_dimension)
          return result.embeddings
        raw = await asyncio.to_thread(_call)
        return [_l2_normalize(v) for v in raw]

    Security: api_key stored only inside self._client — not directly on self. __repr__ returns "VoyageEmbedder(model=..., dim=...)".

    If voyageai.AsyncClient is available and has output_dimension support (Open Question 1), prefer it over asyncio.to_thread by using: self._async_client = voyageai.AsyncClient(api_key=api_key) and await self._async_client.embed(...). Only do this if AsyncClient.embed() accepts output_dimension — otherwise keep to_thread.
  </action>
  <verify>
    <automated>cd /c/Users/HomePC/Documents/GitHub/memoryAgent && uv run --extra dev --extra cloud python -c "from mnema.adapters.embedding.voyage import VoyageEmbedder; v = VoyageEmbedder(api_key='test'); print('VoyageEmbedder ok, dim=', v.dim)"</automated>
  </verify>
  <done>VoyageEmbedder class instantiates with dim property returning 1024; imports without error under cloud extra; conformance embedder_backend voyage param passes when MNEMA_TEST_VOYAGE=1 and real API key set</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QwenEmbedder adapter (PROV-03)</name>
  <files>src/mnema/adapters/embedding/qwen.py</files>
  <behavior>
    - QwenEmbedder(api_key="test", output_dimension=1024) — constructor does not raise
    - qwen_embed.dim == 1024
    - embed() returns L2-normalized vectors of length 1024
    - QwenEmbedder satisfies EmbeddingProvider Protocol
    - Shares dashscope global api_key with QwenLLM safely within single-engine config (Pitfall 4 documented)
  </behavior>
  <read_first>
    src/mnema/adapters/embedding/stub.py — copy class shape, dim property, L2 helper
    04-RESEARCH.md Pattern 4 (lines ~365-391) — DashScope TextEmbedding.call() shape + ASSUMED tag
    04-PATTERNS.md QwenEmbedder section (lines ~167-187) — embed() implementation pattern
    src/mnema/adapters/llm/qwen.py (just created) — dashscope global state handling pattern to follow
  </read_first>
  <action>
    Create src/mnema/adapters/embedding/qwen.py:

    Module docstring: "QwenEmbedder — DashScope Qwen embedding adapter (text-embedding-v4). Satisfies EmbeddingProvider Protocol by structural typing. Direct dashscope SDK (D4-05). L2-normalized at adapter (D4-07). Sync SDK wrapped in asyncio.to_thread (D-13). Warning: dashscope uses module-level global api_key state. QwenLLM and QwenEmbedder MUST share the same api_key in a single-engine config."

    Imports: from __future__ import annotations; import asyncio; import math

    Module-level helper: _l2_normalize() — identical to VoyageEmbedder's helper (same math)

    Class QwenEmbedder (no Protocol inheritance):
      __init__(self, api_key: str, output_dimension: int = 1024) -> None:
        import dashscope  # noqa: PLC0415
        from dashscope import TextEmbedding  # noqa: PLC0415
        dashscope.api_key = api_key
        self._dim = output_dimension
        self._TextEmbedding = TextEmbedding  # hold reference

      dim property -> int: return self._dim

      async embed(self, texts: list[str]) -> list[list[float]]:
        TextEmbedding = self._TextEmbedding
        dim = self._dim
        def _call() -> list[list[float]]:
          try:
            model = TextEmbedding.Models.text_embedding_v4
          except AttributeError:
            model = "text-embedding-v4"   # fallback if Models enum not present (A7)
          resp = TextEmbedding.call(model=model, input=texts, dimension=dim)
          if resp is None or resp.output is None:
            raise ValueError(f"QwenEmbedder: null response from DashScope (dim={dim})")
          return [item["embedding"] for item in resp.output["embeddings"]]
        raw = await asyncio.to_thread(_call)
        return [_l2_normalize(v) for v in raw]

    After writing, run the hermetic suite to confirm no regression. Both embedding adapters are in the cloud extra only — their absence at import does not affect the local stack.
  </action>
  <verify>
    <automated>cd /c/Users/HomePC/Documents/GitHub/memoryAgent && uv run --extra dev --extra cloud python -c "from mnema.adapters.embedding.qwen import QwenEmbedder; q = QwenEmbedder(api_key='test'); print('QwenEmbedder ok, dim=', q.dim)"</automated>
  </verify>
  <done>QwenEmbedder class instantiates with dim=1024; imports without error under cloud extra; hermetic suite still passes; conformance embedder_backend qwen_embed param passes when MNEMA_TEST_DASHSCOPE=1 and real key set</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/env → VoyageEmbedder/QwenEmbedder | API keys never hardcoded; passed via constructor from config |
| embed() output → MemoryEngine | Normalized vectors asserted at conformance level (|norm - 1| < 0.01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | VoyageEmbedder api_key | mitigate | api_key stored inside voyageai.Client only; __repr__ excludes key; adapter satisfies ASVS V2 (no key in logs) |
| T-04-03-02 | Information Disclosure | QwenEmbedder api_key via dashscope global | mitigate | Same as QwenLLM: document global-state risk; single-engine config means one key per process; PROV-02 independent axis is preserved (key shared between Qwen LLM+embed, not between Qwen and Voyage) |
| T-04-03-03 | Tampering | dim mismatch between embedder and t1 | mitigate | MemoryEngine.__init__ startup assertion fires before any data is written (already present); both adapters expose self._dim and dim property so the assertion works correctly |
| T-04-03-04 | Denial of Service | asyncio.to_thread blocking | accept | Same as LLM adapters: thread-pool, not event loop; acceptable for v1 per D-13 |
</threat_model>

<verification>
1. `cd /c/Users/HomePC/Documents/GitHub/memoryAgent && uv run --extra dev pytest tests/ -q 2>&1 | tail -5` — hermetic suite passes
2. `uv run --extra dev --extra cloud python -c "from mnema.adapters.embedding.voyage import VoyageEmbedder; from mnema.adapters.embedding.qwen import QwenEmbedder; print('both embedding adapters import ok')"` — no ImportError
3. `uv run --extra dev pytest tests/conformance/test_embedding_contract.py -q -v 2>&1 | tail -10` — stub param PASSED; voyage/qwen_embed params SKIPPED
4. `uv run --extra dev pyright 2>&1 | tail -3` — 0 errors
</verification>

<success_criteria>
- VoyageEmbedder: dim property returns configured output_dimension (default 1024); embed() returns L2-normalized unit vectors of length dim; uses asyncio.to_thread or AsyncClient; no LiteLLM
- QwenEmbedder: dim property returns output_dimension; embed() normalized; fallback for TextEmbedding.Models vs string literal (A7 assumption handled)
- Both satisfy EmbeddingProvider Protocol by structural typing (pyright strict)
- Independent axis preserved: VoyageEmbedder can pair with AnthropicLLM in a single MemoryEngine config
- Hermetic suite still passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-cloud-providers-backends/04-03-SUMMARY.md`
</output>
