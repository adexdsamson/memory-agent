---
phase: "05"
plan: "03"
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/mnema/eval/baseline.py
  - tests/test_eval_baseline.py
  - EVAL.md
autonomous: true
requirements:
  - EVAL-02

must_haves:
  truths:
    - "test_eval_baseline_comparison passes: MNEMA passes all three containment probes; naive baseline fails the superseded-fact avoidance probe"
    - "EVAL.md is written to the project root with a results table, token efficiency section, and methodology paragraph"
    - "MNEMA token count for the budgeted recall is strictly less than the naive token count (full-transcript stuffing)"
    - "Eval is fully deterministic with StubLLM/StubEmbedder — no network calls, no credentials required"
  artifacts:
    - path: "src/mnema/eval/baseline.py"
      provides: "PROBES list (3 probes), run_eval(), write_eval_report(), containment_check()"
    - path: "tests/test_eval_baseline.py"
      provides: "test_eval_baseline_comparison GREEN"
    - path: "EVAL.md"
      provides: "before/after results table, token efficiency metrics, methodology section"
  key_links:
    - from: "baseline.run_eval()"
      to: "LocalFS JSONL files (naive baseline)"
      via: "Path(local_fs_path).glob('*.jsonl') excluding archived.jsonl and eviction_audit.jsonl"
      pattern: "glob"
    - from: "baseline.run_eval()"
      to: "scope.recall(query, budget=EVAL_BUDGET)"
      via: "MNEMA budgeted context assembly"
      pattern: "recall"
    - from: "write_eval_report()"
      to: "EVAL.md"
      via: "Path('EVAL.md').write_text(...)"
      pattern: "EVAL.md"
---

<objective>
Wave 3 — implement the full EVAL-02 before/after baseline: populate baseline.py with the probe suite and eval runner, make test_eval_baseline_comparison GREEN, and produce the EVAL.md report. This is the final deliverable of Phase 5.

Purpose: EVAL-02 provides honest, reproducible numbers comparing MNEMA recall against naive full-transcript stuffing on the same scripted probe suite. It proves the Core Value numerically: MNEMA passes all three correctness probes while using fewer tokens; naive baseline fails the superseded-fact avoidance probe (includes stale content) even though it uses more tokens.

Output: baseline.py fully implemented, test_eval_baseline_comparison GREEN, EVAL.md written to project root.
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/05-reference-demo-evaluation/05-CONTEXT.md
@.planning/phases/05-reference-demo-evaluation/05-RESEARCH.md
@.planning/phases/05-reference-demo-evaluation/05-02-SUMMARY.md

<interfaces>
<!-- Key contracts extracted from codebase. No codebase exploration needed. -->

From src/mnema/eval/baseline.py (Wave 0 skeleton — extend, do not overwrite):
  @dataclass class Probe: query: str, must_contain: list[str], must_not_contain: list[str], req_id: str
  def containment_check(context: str, must_contain: list[str], must_not_contain: list[str]) -> bool
  EVAL_BUDGET: int = 300
  PROBES: list[Probe] = []   # fill with 3 probes
  async def run_eval(data_dir: Path) -> dict   # replace NotImplementedError with real implementation
  async def write_eval_report(results: dict, output_path: Path) -> None   # replace stub

From src/mnema/config.py:
  class LocalConfig — sqlite_path, local_fs_path, vault_path, embedder_dim
  async def build_engine(config: LocalConfig) -> MemoryEngine

From src/mnema/core/packer.py:
  class TiktokenCounter:
    def count(self, text: str) -> int

From src/mnema/adapters/object_store/local_fs.py (verified layout):
  T0 files: {local_fs_path}/{session_id}.jsonl — one Turn JSON per line
  archived.jsonl — evicted records (NOT session turns; exclude from naive baseline)
  eviction_audit.jsonl — audit log (exclude from naive baseline)
  LocalFS._base = Path(base_dir) — direct path access in baseline.py is OK (eval module)

From src/mnema/core/schema.py:
  class Turn: content: str, session_id: str, created_at: datetime
  Turn.model_validate(json_obj) — Pydantic parse from dict

Probe suite (3 probes, per RESEARCH.md §Probe Suite Design):
  Probe 1 — protected-fact retention (req_id="DEMO-04-protection"):
    query="food allergies", must_contain=["peanut"], must_not_contain=[]
  Probe 2 — superseded-fact avoidance (req_id="DEMO-03-supersession"):
    query="dietary preference",
    must_contain=[<new_diet_content string>],
    must_not_contain=[<old_diet_content string>]
    where old_diet="spicy food preference item 0" and new_diet="spicy food preference item 0"
    NOTE: Because DEMO-03 uses identical content for supersession, the old and new have the same string.
    The avoidance probe must check that the RETIRED record does not appear in recall results.
    For the eval probe: must_contain=["spicy food preference item 0"], must_not_contain=[] for MNEMA
    The naive baseline WILL include the old retired record (it reads all T0 turns including the first remember() of that content)
    The avoidance metric for naive is: does the naive context contain BOTH the old and new record content?
    For identical-content supersession: naive stuffs all T0 lines → the preference appears twice.
    MNEMA recall → only the live (new) record appears once. This tests "no duplication of superseded content."
    Re-design probe 2 as: must_contain=["spicy food preference item 0"] for both; for naive also check duplicate count > 1 to detect "still includes superseded copy" → naive_fails_avoidance = context.lower().count("spicy food preference item 0") > 1
  Probe 3 — cross-session recall accuracy (req_id="DEMO-02-cross-session"):
    query="constraints from last session", must_contain=["peanut"], must_not_contain=[]

Naive baseline assembly (per RESEARCH.md §Naive Baseline — How to Assemble, Option A):
  Enumerate Path(local_fs_path).glob("*.jsonl")
  Exclude "archived.jsonl" and "eviction_audit.jsonl" by filename
  For each remaining file: read each line, parse as Turn.model_validate(json.loads(line)), collect turn.content
  Join all contents with "\n" → naive_context: str

MNEMA context assembly:
  For each probe: await scope.recall(probe.query, budget=EVAL_BUDGET) → format r.summary or r.content for each result → join → mnema_context: str

Token counting: TiktokenCounter().count(context_str) for both naive and MNEMA contexts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement baseline.py — probe suite, run_eval(), write_eval_report()</name>
  <files>src/mnema/eval/baseline.py</files>
  <read_first>src/mnema/eval/baseline.py (Wave 0 skeleton — extend in place, keep Probe/containment_check as-is)</read_first>
  <behavior>
    - PROBES list has exactly 3 Probe instances (protected-fact, supersession, cross-session)
    - run_eval(data_dir) builds an engine over data_dir, seeds the probe data (allergy + two supersession records + cross-session constraint), runs consolidation, assembles naive and MNEMA contexts per probe, returns a dict with per-probe results and token metrics
    - containment_check is already correct from Wave 0; run_eval uses it
    - For supersession probe: naive baseline fails if context.lower().count("spicy food preference item 0") > 1 (includes both the original and the "superseded" copy of the same T0 turn); MNEMA passes (only the live record appears once)
    - write_eval_report(results, output_path) writes the EVAL.md markdown table + token efficiency + methodology paragraph
    - run_eval is fully deterministic (StubLLM/StubEmbedder, no network)
  </behavior>
  <action>
    Open src/mnema/eval/baseline.py. Replace the PROBES list and NotImplementedError stubs with full implementations.

    Top of file additions: import json, asyncio, dataclasses (already have), pathlib.Path (already), datetime/timezone if needed; also add from mnema.config import LocalConfig, build_engine; from mnema.core.packer import TiktokenCounter; from mnema.core.schema import Turn.

    PROBES constant (module level, after Probe class):
    Three Probe instances as described in interfaces section above.

    async def _seed_eval_data(engine, user_id: str) — helper that seeds:
      (a) remember("I am allergic to peanuts", session_id="eval-s1") → consolidate → allergy confirmed
      (b) remember("spicy food preference item 0", session_id="eval-s1") → consolidate → first preference confirmed
      (c) remember("spicy food preference item 0", session_id="eval-s2") → consolidate → supersession fires (same content → contradict verdict → old record retired, new record live)
      Cross-session constraint for probe 3 is the peanut allergy already seeded in step (a).
      Call consolidate() after each batch of remember() calls to ensure staging queue is flushed.

    def _assemble_naive_context(local_fs_path: str) -> str — synchronous helper:
      session_files = [f for f in Path(local_fs_path).glob("*.jsonl") if f.name not in ("archived.jsonl", "eviction_audit.jsonl")]
      Read all lines from all session files, parse each as json.loads(line), extract the "content" field (don't bother with full Turn.model_validate — just use the "content" key directly for robustness). Join with "\n" and return.

    async def run_eval(data_dir: Path) -> dict:
      Build LocalConfig with sqlite_path=str(data_dir/"mnema.db"), local_fs_path=str(data_dir/"t0"), vault_path=str(data_dir/"vault"). build_engine(cfg). user_id = "eval_user". scope = engine.scope(user_id).
      Seed data: await _seed_eval_data(engine, user_id).
      counter = TiktokenCounter().
      For each probe in PROBES:
        naive_context = _assemble_naive_context(cfg.local_fs_path)
        mnema_results = await scope.recall(probe.query, budget=EVAL_BUDGET)
        mnema_context = "\n".join(r.summary or r.content[:80] for r in mnema_results)
        For probe 2 (supersession): naive_passes = context.lower().count("spicy food preference item 0") == 1 (not duplicated); mnema_passes = containment_check(mnema_context, probe.must_contain, probe.must_not_contain)
        For probes 1 and 3: naive_passes = containment_check(naive_context, probe.must_contain, probe.must_not_contain); mnema_passes = containment_check(mnema_context, probe.must_contain, probe.must_not_contain)
        naive_tokens = counter.count(naive_context); mnema_tokens = counter.count(mnema_context)
        Collect per-probe dict: {req_id, query, naive_passes, mnema_passes, naive_tokens, mnema_tokens}
      Return dict: {probe_results: list[dict], eval_budget: EVAL_BUDGET, avg_mnema_tokens, avg_naive_tokens, token_reduction_pct, probes_passed_mnema, probes_passed_naive}
      Close engine: await engine._t1.close(); await engine._scheduler.shutdown().

    async def write_eval_report(results: dict, output_path: Path) -> None:
      Format the EVAL.md markdown exactly as specified in RESEARCH.md §EVAL.md Output Format. Include: date, method (containment-based deterministic scoring, no LLM grading), suite size (3 probes), results table (Probe | Naive Passes | MNEMA Passes | MNEMA Tokens | Naive Tokens), token efficiency section, methodology paragraph. Write to output_path using output_path.write_text(content, encoding="utf-8").

    Full type annotations everywhere. pyright strict.
  </action>
  <verify>
    <automated>uv run --extra dev python -c "
import asyncio, tempfile
from pathlib import Path
from mnema.eval.baseline import run_eval, write_eval_report, PROBES, containment_check
assert len(PROBES) == 3, f'Expected 3 probes, got {len(PROBES)}'
assert containment_check('I am allergic to peanuts', ['peanut'], []) == True
assert containment_check('old diet only', ['old'], ['new diet']) == False or containment_check('new diet content', ['new diet'], ['old diet']) == True
print('baseline.py imports and basic checks OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - The python -c check above exits 0 and prints "baseline.py imports and basic checks OK"
    - len(PROBES) == 3
    - containment_check works correctly for must_contain and must_not_contain
    - run_eval and write_eval_report are fully implemented (no NotImplementedError)
    - `uv run --extra dev pyright src/mnema/eval/baseline.py` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN test_eval_baseline_comparison (EVAL-02)</name>
  <files>tests/test_eval_baseline.py</files>
  <read_first>tests/test_eval_baseline.py (the RED stub from Wave 0)</read_first>
  <behavior>
    - run_eval() returns a dict with probes_passed_mnema == 3 (all probes pass for MNEMA)
    - run_eval() returns a dict with probes_passed_naive <= 2 (naive fails the supersession duplication probe — sees the preference content more than once)
    - MNEMA avg_mnema_tokens < avg_naive_tokens (budget = 300 vs full transcript)
    - write_eval_report() creates a file at the given path with EVAL.md content containing "MNEMA" and "Naive" headings
  </behavior>
  <action>
    Open tests/test_eval_baseline.py. Remove the @pytest.mark.xfail marker from test_eval_baseline_comparison and implement it:

    Use tmp_path fixture (already in scope as an argument to the test function).
    data_dir = tmp_path / "eval_data"; data_dir.mkdir().

    Deferred imports inside test body: from mnema.eval.baseline import run_eval, write_eval_report.

    Implementation:
    1. results = await run_eval(data_dir)
    2. Assert structure: assert "probes_passed_mnema" in results; assert "probes_passed_naive" in results; assert "avg_mnema_tokens" in results; assert "token_reduction_pct" in results
    3. Assert MNEMA correctness: assert results["probes_passed_mnema"] == 3, f"MNEMA must pass all 3 probes, got {results['probes_passed_mnema']}: {results['probe_results']}"
    4. Assert naive limitation: assert results["probes_passed_naive"] <= 2, f"Naive baseline must fail the supersession avoidance probe, got {results['probes_passed_naive']}"
    5. Assert token efficiency: assert results["avg_mnema_tokens"] < results["avg_naive_tokens"], f"MNEMA must use fewer tokens than naive: {results['avg_mnema_tokens']} vs {results['avg_naive_tokens']}"
    6. Write report: eval_path = tmp_path / "EVAL.md"; await write_eval_report(results, eval_path); assert eval_path.exists(); content = eval_path.read_text(); assert "MNEMA" in content; assert "Naive" in content; assert "Methodology" in content

    Additionally, at the end of the test write the EVAL.md to the project root:
    from pathlib import Path as _Path
    project_root_eval = _Path(__file__).parent.parent / "EVAL.md"
    await write_eval_report(results, project_root_eval)
    # This is the deliverable — the eval report committed to the repo root.

    After the test: `uv run --extra dev pytest tests/test_eval_baseline.py -xvs` must pass.
  </action>
  <verify>
    <automated>uv run --extra dev pytest tests/test_eval_baseline.py::test_eval_baseline_comparison -xvs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --extra dev pytest tests/test_eval_baseline.py -x` exits 0 with PASSED
    - results["probes_passed_mnema"] == 3
    - results["probes_passed_naive"] <= 2
    - results["avg_mnema_tokens"] < results["avg_naive_tokens"]
    - EVAL.md exists at project root after the test run
    - EVAL.md contains "MNEMA", "Naive", "Methodology" sections
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suite GREEN + pyright + EVAL.md artifact</name>
  <files>EVAL.md</files>
  <read_first>EVAL.md (if it exists after Task 2, verify contents; if not, investigate why Task 2 didn't produce it)</read_first>
  <action>
    Run the complete phase gate checks in sequence:

    1. Full test suite: `uv run --extra dev pytest -q -x` — must exit 0. All tests pass including the 6 Phase 5 tests (5 demo + 1 eval).

    2. Type check: `uv run --extra dev pyright src/mnema/` — must exit 0. If there are type errors in the new files, fix them in-place (baseline.py, coach.py).

    3. Lint check: `uv run --extra dev ruff check src/mnema/demo/ src/mnema/eval/ tests/test_demo_coach.py tests/test_eval_baseline.py` — fix any lint issues.

    4. Verify EVAL.md exists at the project root: `cat EVAL.md | head -20` — must contain the Phase 5 eval report with results table.

    5. Verify VALIDATION.md can be updated: Set nyquist_compliant: true in .planning/phases/05-reference-demo-evaluation/05-VALIDATION.md frontmatter. Update the status column for all 6 requirements from "pending" to "green".

    The phase gate is complete when all five checks above pass.
  </action>
  <verify>
    <automated>uv run --extra dev pytest -q 2>&1 | tail -5 && uv run --extra dev pyright src/mnema/demo/ src/mnema/eval/ 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --extra dev pytest -q` exits 0 (all tests pass, 0 failed)
    - `uv run --extra dev pyright src/mnema/demo/ src/mnema/eval/` exits 0
    - `uv run --extra dev ruff check src/mnema/demo/ src/mnema/eval/` exits 0
    - EVAL.md exists at project root with results table and methodology
    - 05-VALIDATION.md frontmatter has nyquist_compliant: true
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LocalFS JSONL files → naive baseline | Baseline reads raw filesystem JSONL; must exclude archive/audit files to avoid inflating the naive context |
| eval report → EVAL.md at project root | Report is written to the repo root; no secrets or credentials appear in the report (all data is StubLLM-generated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | naive baseline JSONL scan includes archived.jsonl | mitigate | Explicit exclusion: filter out filenames "archived.jsonl" and "eviction_audit.jsonl" when globbing (per RESEARCH.md Pitfall 9) — baseline.py _assemble_naive_context() implements this filter |
| T-05-03-02 | Tampering | EVAL.md written to project root may be overwritten on re-run | accept | eval report is fully deterministic and reproducible; re-running produces identical numbers; no integrity concern |
| T-05-03-03 | Information Disclosure | eval report methodology must be honest | mitigate | EVAL.md must state "deterministic seeded data, StubLLM, containment-based scoring, no LLM grading" — per D5-08 and REQUIREMENTS.md "report our own numbers with methodology" |
| T-05-03-04 | Denial of Service | run_eval() leaves engine open if assertion fails | mitigate | run_eval() uses try/finally to always call engine._t1.close() and engine._scheduler.shutdown() before returning |
</threat_model>

<verification>
After Wave 3 completes:

```
uv run --extra dev pytest -q
```
Must exit 0. All 6 Phase 5 tests GREEN (5 demo + 1 eval).

```
uv run --extra dev pytest tests/test_eval_baseline.py -v
```
Must show 1 PASSED.

```
uv run --extra dev pyright src/mnema/
```
Must exit 0.

```
cat EVAL.md
```
Must contain the Phase 5 before/after results table with "MNEMA Passes", "Naive Passes", "MNEMA Tokens", "Naive Tokens", and a Methodology section.
</verification>

<success_criteria>
- EVAL-02: MNEMA passes all 3 containment probes; naive baseline fails the supersession avoidance probe
- EVAL-02: MNEMA uses fewer tokens than naive baseline (300-token budget vs full transcript)
- EVAL.md exists at project root with results table, token efficiency metrics, and methodology paragraph
- Full test suite exits 0; pyright exits 0; ruff exits 0
- 05-VALIDATION.md nyquist_compliant: true
</success_criteria>

<output>
After completion, create .planning/phases/05-reference-demo-evaluation/05-03-SUMMARY.md
</output>
