---
phase: 01-schema-ports-local-core-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/mnema/__init__.py
  - src/mnema/py.typed
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_remember_recall.py
  - tests/test_scope_isolation.py
  - tests/test_write_path.py
  - tests/test_providers.py
  - tests/test_scheduler.py
  - tests/test_sdk_interface.py
autonomous: true
requirements:
  - EVAL-01
  - IFACE-01
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
  - CORE-05

must_haves:
  truths:
    - "uv run pytest tests/ -x -q exits with collection errors (RED), not import errors"
    - "uv run pyright exits 0 on the scaffold (no source files yet means no type errors)"
    - "pyproject.toml contains [tool.pytest.ini_options] with asyncio_mode = auto"
    - "uv run mnema --help or python -c 'import mnema' succeeds after uv sync"
    - "All five test files exist with at least one async test function per Phase 1 success criterion"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv project manifest with runtime deps, dev deps, pytest + pyright config"
      contains: "asyncio_mode"
    - path: "src/mnema/__init__.py"
      provides: "importable mnema package"
    - path: "tests/conftest.py"
      provides: "shared fixtures: in-memory engine, StubEmbedder, tmp_path T0"
    - path: "tests/test_remember_recall.py"
      provides: "5-scenario harness (EVAL-01)"
    - path: "tests/test_scope_isolation.py"
      provides: "cross-user isolation assertion (SC-1 scope)"
  key_links:
    - from: "pyproject.toml"
      to: "src/mnema/"
      via: "packages = [{include = 'mnema', from = 'src'}]"
      pattern: "include.*mnema.*src"
    - from: "tests/conftest.py"
      to: "mnema.MemoryEngine"
      via: "async fixture engine(tmp_path)"
      pattern: "async def engine"
---

<objective>
Bootstrap the uv project, the installable `mnema` package skeleton, the pytest/pyright harness, and all test files with RED (failing) stubs that map 1-to-1 to the 5 ROADMAP Phase 1 success criteria.

Purpose: The Walking Skeleton rule requires failing end-to-end tests to exist BEFORE any implementation. This plan establishes the wave-0 test infrastructure so that Plans 02–05 have concrete RED assertions to turn GREEN. Pyright strict is configured here so it runs on every subsequent plan's output.

Output: `pyproject.toml`, `src/mnema/__init__.py` stub, `src/mnema/py.typed` marker, `tests/conftest.py` with fixtures, and six test files with RED stubs. Tests fail on ImportError (expected — implementation does not exist yet), not on assertion errors.
</objective>

<execution_context>
@C:/Users/HomePC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HomePC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-schema-ports-local-core-foundation/01-CONTEXT.md
@.planning/phases/01-schema-ports-local-core-foundation/01-RESEARCH.md
@.planning/phases/01-schema-ports-local-core-foundation/01-VALIDATION.md
@mnema-build-plan.md

<interfaces>
<!-- No source files exist yet — this is the greenfield scaffold plan. -->
<!-- The test files define the EXPECTED public surface for later plans to implement against. -->

Expected public SDK surface (consumed by tests in this plan, implemented in Plan 04):
  from mnema import MemoryEngine, ScopedHandle

  MemoryEngine(
      embedder: EmbeddingProvider,
      t1: RecordStore & VectorIndex,   # SqliteT1 satisfies both
      t0: ObjectStorePort,
      scheduler: Scheduler,
  ) -> MemoryEngine

  await engine.remember(content: str, *, user_id: str, session_id: str, agent_id: str | None = None) -> str
  await engine.recall(query: str, *, user_id: str, agent_id: str | None = None) -> list[MemoryRecord]
  await engine.expand(record_id: str, *, user_id: str) -> Turn | None
  await engine.forget(record_id: str, *, user_id: str, reason: str = "") -> None
  await engine.consolidate(*, force: bool = False) -> None
  engine.scope(user_id: str, agent_id: str | None = None) -> ScopedHandle

  ScopedHandle:
    await .remember(content: str, *, session_id: str, durable: bool = False) -> str
    await .recall(query: str) -> list[MemoryRecord]
    await .expand(record_id: str) -> Turn | None

Expected adapter factories (implemented in Plans 02–03):
  from mnema.adapters.vector_store.sqlite_t1 import SqliteT1
  SqliteT1.open(db_path: str, dim: int) -> Awaitable[SqliteT1]   # classmethod

  from mnema.adapters.embedding.stub import StubEmbedder
  StubEmbedder(dim: int = 128)

  from mnema.adapters.object_store.local_fs import LocalFS
  LocalFS(base_dir: str)

  from mnema.adapters.scheduler.in_process import InProcessScheduler
  InProcessScheduler()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: uv project scaffold and pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>
    - mnema-build-plan.md (§9 — tech stack)
    - .planning/phases/01-schema-ports-local-core-foundation/01-RESEARCH.md (Standard Stack section, Pattern 7 pytest-asyncio config, Environment Availability section)
    - CLAUDE.md (Technology Stack — Python 3.12+, uv, ruff, pyright, pytest + pytest-asyncio)
  </read_first>
  <behavior>
    - `uv run python -c "import mnema"` exits 0 after `uv sync`
    - `uv run pytest --collect-only` runs without ImportError on the test files (tests may fail, not error)
    - `uv run pyright` exits 0 (no source to check yet)
    - `uv run ruff check src/` exits 0 on an empty src/mnema/__init__.py
  </behavior>
  <action>
    Initialize the uv project as a src-layout library. The pyproject.toml must configure:

    [project] name="mnema", version="0.1.0", requires-python=">=3.12", description="Portable provider-agnostic memory engine for AI agents". Runtime dependencies: pydantic>=2.12, aiosqlite>=0.22, sqlite-vec>=0.1.9, numpy>=2.4, "apscheduler>=3.11,<4". Do NOT add apscheduler==4.x.

    [project.optional-dependencies] dev = ["pytest>=8", "pytest-asyncio>=1.4", "ruff", "pyright"].

    [build-system] requires=["hatchling"], build-backend="hatchling.build".

    [tool.hatch.build.targets.wheel] packages = ["src/mnema"].

    [tool.pytest.ini_options] asyncio_mode = "auto", asyncio_default_fixture_loop_scope = "function", testpaths = ["tests"].

    [tool.pyright] pythonVersion = "3.12", typeCheckingMode = "strict", include = ["src/mnema"], venvPath = ".", venv = ".venv".

    [tool.ruff.lint] select = ["E", "F", "I"], line-length = 100.

    Create `src/mnema/__init__.py` as an empty file (or with a single `# mnema` comment). Create `src/mnema/py.typed` (empty marker file for PEP 561). Create `tests/__init__.py` as an empty file. Run `uv sync --all-extras` to install.
  </action>
  <verify>
    <automated>uv run python -c "import mnema; print('ok')" && uv run pytest --collect-only -q 2>&1 | head -20 && uv run pyright --version</automated>
  </verify>
  <done>uv sync succeeds, `import mnema` works, `uv run pytest --collect-only` runs without crashing (test files not yet created — zero collected is acceptable), pyproject.toml has asyncio_mode = "auto"</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test infrastructure — conftest and RED test stubs</name>
  <files>
    tests/conftest.py
    tests/test_remember_recall.py
    tests/test_scope_isolation.py
    tests/test_write_path.py
    tests/test_providers.py
    tests/test_scheduler.py
    tests/test_sdk_interface.py
  </files>
  <read_first>
    - .planning/phases/01-schema-ports-local-core-foundation/01-RESEARCH.md (Pattern 7 pytest-asyncio config, section "5-test harness structure", section "Validation Architecture / Wave 0 Gaps")
    - .planning/ROADMAP.md (Phase 1 Success Criteria 1–5 — these are the 5 test scenarios)
    - .planning/phases/01-schema-ports-local-core-foundation/01-VALIDATION.md (Per-Task Verification Map, Wave 0 Requirements)
    - mnema-build-plan.md (§6 MCP tool contracts — `remember`/`recall`/`expand` I/O shapes)
    - .planning/phases/01-schema-ports-local-core-foundation/01-CONTEXT.md (D-01/D-02/D-03 — scope kwarg signatures)
  </read_first>
  <behavior>
    - `uv run pytest tests/ -x -q` exits with ImportError failures (RED — no implementation yet), NOT with SyntaxError or assertion errors from badly written stubs
    - Each test file has at least one `async def test_*` function
    - `tests/conftest.py` defines an `engine` async fixture that constructs `MemoryEngine` from the four adapters — even though the imports fail now, the fixture structure is correct
    - `tests/test_remember_recall.py` has exactly 5 test functions mapping to SC-1 through SC-5
    - `tests/test_scope_isolation.py` has at least one test asserting `len(results) == 0` when recalling with a different `user_id`
    - `tests/test_write_path.py` has a test that asserts the fast write path does NOT invoke a real LLM (uses a flag or mock)
    - `tests/test_providers.py` has a test asserting that creating `MemoryEngine` with a mismatched `embedding_dim` raises `ValueError`
    - `tests/test_scheduler.py` has a test calling `scheduler.trigger_now()` and asserting the consolidate stub was invoked
    - `tests/test_sdk_interface.py` has a test importing from `mnema` and asserting `MemoryEngine`, `ScopedHandle` are accessible
  </behavior>
  <action>
    Write `tests/conftest.py`. The file imports from `mnema`, `mnema.adapters.vector_store.sqlite_t1.SqliteT1`, `mnema.adapters.embedding.stub.StubEmbedder`, `mnema.adapters.object_store.local_fs.LocalFS`, `mnema.adapters.scheduler.in_process.InProcessScheduler`. Define:
    - `@pytest.fixture async def stub_embedder() -> StubEmbedder` — returns `StubEmbedder(dim=128)`
    - `@pytest.fixture async def engine(tmp_path, stub_embedder)` — constructs `SqliteT1.open(":memory:", dim=stub_embedder.dim)`, `LocalFS(str(tmp_path / "t0"))`, `InProcessScheduler()` (start it), `MemoryEngine(embedder=stub_embedder, t1=t1, t0=t0, scheduler=scheduler)`. Yields engine, then calls `scheduler.shutdown()`.

    Write `tests/test_remember_recall.py` with 5 async test functions (named exactly as below, docstrings reference the SC number):
    - `test_remember_and_recall_scoped` (SC-1): calls `engine.remember("I am allergic to peanuts", user_id="u1", session_id="s1")`, then `engine.recall("food allergies", user_id="u1")`, asserts at least one result contains "peanut" in content or summary (case-insensitive).
    - `test_cross_session_provisional_recall` (SC-2): calls `engine.remember("I love spicy food", user_id="u1", session_id="s1")` then `engine.recall("spicy preferences", user_id="u1")` — asserts result is found even though consolidation has not run (provisional T1 write must surface it).
    - `test_within_session_buffer_freshness` (SC-2): uses `engine.scope(user_id="u1")` for ScopedHandle; calls `scope.remember("I hate mushrooms", session_id="s2")` then `scope.recall("food preferences")` in the same session — asserts "mushroom" appears in results (buffer must surface it).
    - `test_fast_write_schema_columns` (SC-3): calls `engine.remember("I prefer vegan meals", user_id="u1", session_id="s1")` then fetches the record via the `t1` adapter directly; asserts `embedding_model is not None`, `embedding_dim is not None`, `isinstance(record.protected, bool)`, `record.valid_until is None`.
    - `test_expand_and_access_count` (SC-5): calls `engine.remember("I batch-cook on Sundays", user_id="u1", session_id="s1")`; recalls, takes `results[0]`; asserts `results[0].access_count >= 1`; if `results[0].t0_ref` is not None, calls `engine.expand(results[0].id, user_id="u1")` and asserts the return is not None.

    Write `tests/test_scope_isolation.py` with 2 tests:
    - `test_recall_does_not_leak_across_users`: stores a record under `user_id="u1"`, recalls with `user_id="u2"`, asserts `len(results) == 0`.
    - `test_user_id_required_kwarg`: asserts that calling `engine.recall("query")` without `user_id` raises `TypeError` (the kwarg is non-defaulted).

    Write `tests/test_write_path.py` with 2 tests:
    - `test_durable_claim_produces_t1_record`: stores "I am diabetic" (a fact-class durable claim), then directly queries `t1` for live records for `user_id`, asserts at least one record exists with `provisional=True`.
    - `test_non_durable_claim_skips_t1`: stores "What is the weather today?" (question — heuristic suppresses), then queries `t1`, asserts 0 records for that user.

    Write `tests/test_providers.py` with 1 test:
    - `test_dim_mismatch_raises_at_startup`: constructs a `StubEmbedder(dim=128)`, creates `SqliteT1.open(":memory:", dim=64)` (different dim), then constructs `MemoryEngine(embedder=stub_embedder_128, t1=t1_64, ...)` and asserts `ValueError` is raised (dim mismatch assertion at startup, per PROV-06).

    Write `tests/test_scheduler.py` with 1 test:
    - `test_trigger_now_fires_consolidate`: creates `InProcessScheduler()`, calls `scheduler.start()`, schedules a sentinel function via `scheduler.schedule(fn, every_seconds=3600)`, calls `scheduler.trigger_now()`, uses `asyncio.sleep(0.2)` to wait, asserts the sentinel was called.

    Write `tests/test_sdk_interface.py` with 1 test:
    - `test_public_surface_importable`: asserts `from mnema import MemoryEngine, ScopedHandle` works (i.e., both names exist in the mnema namespace).

    All test files must be syntactically valid Python with correct `import pytest` and `import asyncio` at the top where used. No fenced code blocks: these are complete, real test function bodies, not stubs that raise `NotImplementedError` — the bodies are real assertions that will fail with ImportError until the implementations exist.
  </action>
  <verify>
    <automated>uv run pytest tests/ --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>All test files collected (no SyntaxError). `uv run pytest tests/ -x -q` exits non-zero (RED) due to ImportError — not SyntaxError. At least 13 test functions collected across all 6 test files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → mnema SDK | Tests call remember/recall with controlled user_ids; trust boundary is the scope kwarg enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | `engine.recall()` — missing `user_id` predicate | mitigate | `user_id` is a non-defaulted kwarg at the Protocol level; omission raises `TypeError` before any DB access. `test_user_id_required_kwarg` in `test_scope_isolation.py` asserts this at the test layer. |
| T-1-02 | Tampering | pyproject.toml dependency resolution — apscheduler 4.x alpha being pulled | mitigate | Pin `"apscheduler>=3.11,<4"` in pyproject.toml. `uv lock` will fail on any 4.x resolution. |
| T-1-03 | Denial of Service | Test harness importing real network-calling embedder | mitigate | conftest.py fixture uses only `StubEmbedder` — no network calls in any test. |
</threat_model>

<verification>
- `uv run python -c "import mnema"` exits 0
- `uv run pytest --collect-only -q` shows >= 13 collected test items with no collection errors
- `uv run pytest tests/ -x -q` exits non-zero (tests are RED, not erroring on syntax)
- `pyproject.toml` grep for `asyncio_mode` returns "asyncio_mode = "auto""
- `pyproject.toml` grep for `apscheduler` returns a line containing `<4`
</verification>

<success_criteria>
- uv project installs cleanly with `uv sync`
- `import mnema` works
- All 6 test files exist and are syntactically valid
- pytest collects >= 13 test functions
- All tests are RED (ImportError), not erroring on syntax
- pyright is configured in strict mode
- `apscheduler>=3.11,<4` is in the dependency list
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-ports-local-core-foundation/01-01-SUMMARY.md` using the template at `@C:/Users/HomePC/.claude/get-shit-done/templates/summary.md`.
</output>
