---
phase: 01-schema-ports-local-core-foundation
plan: "05"
type: execute
wave: 4
depends_on:
  - "04"
files_modified:
  - tests/test_remember_recall.py
  - tests/test_scope_isolation.py
  - tests/test_schema.py
  - tests/conftest.py
  - .planning/phases/01-schema-ports-local-core-foundation/01-VALIDATION.md
autonomous: false
requirements:
  - EVAL-01
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
  - CORE-05
  - RECALL-06
  - RECALL-07

must_haves:
  truths:
    - "uv run pytest tests/ -v exits 0 — all 13+ tests GREEN"
    - "test_scope_isolation.py::test_recall_does_not_leak_across_users: recall with user_id=u2 returns empty when only u1 has records"
    - "test_scope_isolation.py::test_user_id_required_kwarg: engine.recall without user_id raises TypeError"
    - "test_remember_recall.py::test_fast_write_schema_columns: record has embedding_model, embedding_dim set, protected is bool, valid_until is None"
    - "test_remember_recall.py::test_expand_and_access_count: results[0].access_count >= 1 after recall; expand(id) returns non-None Turn"
    - "uv run pytest tests/ -q && uv run pyright exits 0 (full phase gate)"
  artifacts:
    - path: "tests/test_schema.py"
      provides: "Standalone schema unit tests (CORE-02/03/04/05)"
    - path: ".planning/phases/01-schema-ports-local-core-foundation/01-VALIDATION.md"
      provides: "Updated validation map with final pass/fail status"
  key_links:
    - from: "tests/test_remember_recall.py"
      to: "mnema.MemoryEngine.expand"
      via: "test_expand_and_access_count asserts Turn returned verbatim"
      pattern: "expand"
    - from: "tests/test_schema.py"
      to: "mnema.core.schema.MemoryRecord"
      via: "direct Pydantic model construction tests"
      pattern: "MemoryRecord"
---

<objective>
Complete the full 5-test harness (EVAL-01), add a standalone schema unit test file, and confirm that every Phase 1 requirement is GREEN. Fix any remaining test failures, verify the `test_expand_and_access_count` test exercises the real `expand()` code path, and run the full phase gate (`uv run pytest tests/ -q && uv run pyright`).

Purpose: The previous four plans built the system. This plan is the verification phase — confirming that every one of the 5 ROADMAP success criteria has a passing automated test, that the un-retrofittable schema columns are covered by dedicated unit tests, and that pyright strict is clean across the entire tree. A human visual check of the test output confirms the phase is shippable.

Output: Completed test files, a new `tests/test_schema.py`, updated `01-VALIDATION.md` with pass/fail status. Full suite GREEN. This plan requires a human-verify checkpoint to confirm the output before the phase is closed.
</objective>

<execution_context>
@C:/Users/HomePC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HomePC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-schema-ports-local-core-foundation/01-CONTEXT.md
@.planning/phases/01-schema-ports-local-core-foundation/01-RESEARCH.md
@.planning/phases/01-schema-ports-local-core-foundation/01-VALIDATION.md
@.planning/phases/01-schema-ports-local-core-foundation/01-04-SUMMARY.md
@.planning/ROADMAP.md

<interfaces>
<!-- Contracts finalized in Plans 02-04. -->

MemoryEngine public surface (from src/mnema/__init__.py):
  from mnema import MemoryEngine, ScopedHandle

  await engine.remember(content, *, user_id, session_id, agent_id=None, type_hint=None, durable=False) -> str
  await engine.recall(query, *, user_id, agent_id=None, k=30) -> list[MemoryRecord]
  await engine.expand(record_id, *, user_id) -> Turn | None
  engine.scope(user_id, agent_id=None) -> ScopedHandle

SqliteT1 extra method (from Plan 02):
  await t1.get_latest(user_id: str) -> MemoryRecord | None   # used in test_fast_write_schema_columns

Phase 1 success criteria (from ROADMAP.md — these are the 5 tests):
  SC-1: remember + recall returns fact scoped to user_id/agent_id/session_id, no cross-scope leak
  SC-2: durable claim recallable cross-session via provisional T1; same-session via buffer
  SC-3: fast write: T0 + buffer + provisional T1 without LLM, record has all required columns
  SC-4: runs on local adapters (sqlite-vec + local FS + in-process scheduler), LLM/embedding independently configurable, 5-test harness green
  SC-5: expand(id) returns verbatim T0 detail; access_count updated on recall
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test_schema.py and fix any remaining test failures</name>
  <files>
    tests/test_schema.py
    tests/test_remember_recall.py
    tests/test_scope_isolation.py
    tests/conftest.py
  </files>
  <read_first>
    - .planning/phases/01-schema-ports-local-core-foundation/01-RESEARCH.md (Pattern 1 schema — exact field names and types, CORE-01/02/03/04/05 requirements)
    - src/mnema/core/schema.py (MemoryRecord fields — verify they match the DDL)
    - tests/test_remember_recall.py (check which tests are still RED and why)
    - tests/test_scope_isolation.py (check current state)
    - tests/conftest.py (engine fixture — ensure it matches the final MemoryEngine API)
  </read_first>
  <acceptance_criteria>
    - `uv run pytest tests/test_schema.py -x -q` exits 0
    - test_schema.py asserts: `MemoryRecord(user_id="u1", session_id="s1", record_type=RecordType.FACT, content="test")` constructs without error
    - test_schema.py asserts: `record.protected` is False (bool), not 0 (int)
    - test_schema.py asserts: `record.valid_until is None` (CORE-05 — new records are live)
    - test_schema.py asserts: `record.embedding_model is None` at construction (provenance set at write time)
    - test_schema.py asserts: a `MemoryRecord` with `record_type="fact"` is coerced to `RecordType.FACT` (StrEnum)
    - test_schema.py asserts: `record.access_count == 0` at construction (RECALL-07 baseline)
    - All existing tests in test_remember_recall.py and test_scope_isolation.py continue to pass after any fixes
    - `uv run pytest tests/ -x -q` exits 0 after this task
  </acceptance_criteria>
  <action>
    First, run `uv run pytest tests/ -v 2>&1` to see the current state of all tests. Read each failing test's error message. Fix failures by updating the test files OR the implementation, depending on which side has the bug.

    Common failure patterns to investigate:
    - `test_fast_write_schema_columns`: may fail if `t1.get_latest(user_id=...)` is not implemented on SqliteT1. If missing, implement it (or access `engine._t1` and call `get_latest`). The method should SELECT * FROM t1_records WHERE user_id=? ORDER BY created_at DESC LIMIT 1.
    - `test_expand_and_access_count`: may fail if `t0_ref` is None on the record. Ensure WritePath sets `t0_ref` from the LocalFS append return value.
    - `test_within_session_buffer_freshness`: may fail if buffer is not keyed by user_id. If the buffer conflates all users' turns, fix `RecentSessionBuffer` to key by `(user_id, session_id)` and update `RecallPath.execute` to pass user_id when calling `buffer.as_candidates_for_user`.
    - `test_cross_session_provisional_recall`: may fail if the StubEmbedder vectors for "spicy food" query and "I love spicy food" content are too dissimilar. Verify by checking that the dense search returns the record. If k=30 is not returning it, the stub vector for a 1-element database should always return that element. Investigate if the vec0 KNN query requires at least k records to exist.
    - Type errors from pyright: fix the intersection type issue for `t1: RecordStore & VectorIndex`. Define a `T1Adapter` Protocol in `mnema/ports/__init__.py` that extends both, and type `t1: T1Adapter` in MemoryEngine.

    Create `tests/test_schema.py` with the assertions listed in acceptance_criteria. Import `from mnema.core.schema import MemoryRecord, RecordType` directly (not via the engine fixture). All tests in this file are synchronous (no async needed — Pydantic model construction is sync).

    After creating/fixing, run `uv run pytest tests/ -v` to confirm all GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/ -v 2>&1 | tail -20</automated>
  </verify>
  <done>All tests GREEN when run with `uv run pytest tests/ -v`. `tests/test_schema.py` exists with at least 6 assertions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 1 implementation: schema, six Protocol ports, SqliteT1 adapter, LocalFS T0 adapter, StubEmbedder, InProcessScheduler, WritePath, RecallPath, MemoryEngine, ScopedHandle, and the full 5-test harness. All 5 ROADMAP Phase 1 success criteria have passing tests.</what-built>
  <how-to-verify>
    Run the following commands in order and verify each output:

    1. Full test suite (must be all GREEN):
       `uv run pytest tests/ -v`
       Expected: all test functions PASSED, 0 failed, 0 errors

    2. Pyright strict check (must exit 0):
       `uv run pyright`
       Expected: "0 errors, 0 warnings, 0 informations" or similar clean output

    3. Ruff lint (must exit 0):
       `uv run ruff check src/`
       Expected: no output (clean)

    4. Quick smoke test — the end-to-end slice manually:
       `uv run python -c "import asyncio; from mnema import MemoryEngine; from mnema.adapters.embedding.stub import StubEmbedder; from mnema.adapters.vector_store.sqlite_t1 import SqliteT1; from mnema.adapters.object_store.local_fs import LocalFS; from mnema.adapters.scheduler.in_process import InProcessScheduler; import tempfile, os; async def main(): e=StubEmbedder(dim=128); t1=await SqliteT1.open(':memory:', dim=128); t0=LocalFS(tempfile.mkdtemp()); s=InProcessScheduler(); s.start(); eng=MemoryEngine(embedder=e,t1=t1,t0=t0,scheduler=s); await eng.remember('I am allergic to peanuts', user_id='u1', session_id='s1'); results=await eng.recall('food allergies', user_id='u1'); print('Results:', [r.content[:40] for r in results]); s.shutdown(); asyncio.run(main())"`
       Expected: prints at least one result containing "peanut"

    5. Scope isolation check:
       Same as #4 but append `results2=await eng.recall('peanut', user_id='u2'); assert len(results2)==0, f'LEAK: {results2}'` before shutdown.
       Expected: no assertion error

    Confirm the following schema columns exist by checking:
    `uv run python -c "from mnema.core.schema import MemoryRecord, RecordType; r=MemoryRecord(user_id='u1',session_id='s1',record_type=RecordType.FACT,content='x'); print(r.model_fields.keys())"`
    Expected output includes: user_id, session_id, agent_id, embedding_model, embedding_dim, embedding_version, protected, valid_until, access_count, last_accessed
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. If any test is RED or pyright has errors, describe the failure and the executor will fix it before the phase is closed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → full stack | Integration tests exercise the full path from SDK surface to SQLite; no mocks in recall path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-14 | Information Disclosure | test_scope_isolation failing silently — test passes but assertion is wrong | mitigate | `test_recall_does_not_leak_across_users` stores u1 data, recalls as u2, asserts `len(results) == 0`. If the test passes with a wrong assertion, it would assert trueness of an empty list (which is falsy). The assertion is `assert len(results) == 0`, not `assert not results` — this fails if len is wrong. |
| T-1-15 | Tampering | pyright strict disabled / passing without real checks | mitigate | pyproject.toml has `typeCheckingMode = "strict"`. The phase gate command is `uv run pyright` which uses this config. The human-verify checkpoint confirms the 0-error output. |
</threat_model>

<verification>
- `uv run pytest tests/ -v` exits 0, all tests PASSED
- `uv run pyright` exits 0 (0 errors)
- `uv run ruff check src/` exits 0
- End-to-end smoke test returns at least one result containing "peanut"
- Scope isolation check produces no assertion error
- `01-VALIDATION.md` updated with pass status for all requirement IDs
</verification>

<success_criteria>
- All 13+ test functions GREEN
- pyright strict clean across the entire src/mnema/ tree
- ruff clean
- Human has run the smoke test and confirmed the end-to-end slice works
- Phase 1 is shippable: `uv run pytest tests/ -q` is the single command that proves it
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-ports-local-core-foundation/01-05-SUMMARY.md` using the template at `@C:/Users/HomePC/.claude/get-shit-done/templates/summary.md`.

Also update `.planning/ROADMAP.md` Phase 1 entry:
- Set Plans count to "5 plans"
- Add plan list:
  - [x] 01-01-PLAN.md — Project scaffold, pytest harness, RED test stubs
  - [x] 01-02-PLAN.md — Pydantic schema, six Protocol ports, SqliteT1, LocalFS adapters
  - [x] 01-03-PLAN.md — StubEmbedder, InProcessScheduler adapters
  - [x] 01-04-PLAN.md — WritePath, RecallPath, MemoryEngine, ScopedHandle, SDK re-export
  - [x] 01-05-PLAN.md — Full harness GREEN, schema unit tests, phase gate
</output>
