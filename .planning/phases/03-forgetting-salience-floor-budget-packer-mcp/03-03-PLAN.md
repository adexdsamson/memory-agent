---
phase: 03-forgetting-salience-floor-budget-packer-mcp
plan: "03"
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/mnema/ports/vault.py
  - src/mnema/adapters/vault/local_fs_vault.py
  - src/mnema/core/consolidation.py
  - src/mnema/core/engine.py
  - tests/conftest.py
autonomous: true
requirements:
  - CONS-09
  - TIER-03

must_haves:
  truths:
    - "After consolidate(), confirmed high-salience records appear in the T2 vault markdown file"
    - "LocalFSVault writes {base_dir}/{user_id}.md sectioned by record_type with markdown bullet points"
    - "Vault deduplication by summary/content prevents duplicates on repeated consolidation (TIER-03)"
    - "Vault promotion runs BEFORE eviction in ConsolidationPipeline.run() — implemented as TWO SEPARATE iteration loops (vault first, eviction second)"
    - "test_vault_promotion_before_eviction: a record with salience=0.72 (above threshold) AND low keep_score (backdated + zero access) is PROMOTED to vault AND THEN evicted — vault contains the record AND valid_until is set after consolidate()"
    - "user_id in vault filenames validated against alphanumeric+hyphen+underscore pattern (path traversal prevention)"
    - "VAULT_SALIENCE_THRESHOLD = 0.7 module-level constant in consolidation.py"
    - "engine.consolidate(user_id=...) passes user_id to pipeline; consolidate(user_id=None) uses global behavior (all staged users)"
  artifacts:
    - path: "src/mnema/adapters/vault/local_fs_vault.py"
      provides: "LocalFSVault adapter — T2 markdown vault; satisfies VaultStore by structural typing"
      exports: ["LocalFSVault"]
      contains: "LocalFSVault"
    - path: "src/mnema/core/consolidation.py"
      provides: "vault promotion pass (BEFORE eviction pass, separate loops); VAULT_SALIENCE_THRESHOLD; eviction loop replacing bare pass; vault: Any = None and t0: Any = None constructor kwargs; user_id filter on run()"
      contains: "VAULT_SALIENCE_THRESHOLD"
    - path: "src/mnema/core/engine.py"
      provides: "ConsolidationPipeline constructed with vault=self._vault and t0=self._t0; consolidate() accepts optional user_id: str | None = None and passes through to pipeline.run()"
      contains: "vault=self._vault"
    - path: "tests/conftest.py"
      provides: "engine_with_vault fixture for vault integration tests"
      contains: "engine_with_vault"
  key_links:
    - from: "src/mnema/core/consolidation.py run() — vault loop"
      to: "self._vault.promote(record)"
      via: "FIRST loop over live_records: promote qualifying records (non-provisional, salience >= VAULT_SALIENCE_THRESHOLD, valid_until is None)"
      pattern: "vault.promote"
    - from: "src/mnema/core/consolidation.py run() — eviction loop"
      to: "decay_pass + 4-step eviction"
      via: "SECOND loop: async for record, score in decay_pass(...); separate from vault loop"
      pattern: "decay_pass"
    - from: "src/mnema/core/engine.py consolidate()"
      to: "self._consolidation_pipeline.run(user_id=user_id)"
      via: "user_id passed through for per-user scoped consolidation (WARNING 7 fix)"
      pattern: "user_id"
    - from: "src/mnema/adapters/vault/local_fs_vault.py _vault_path()"
      to: "_validate_user_id(user_id)"
      via: "path traversal guard — same regex as LocalFS._validate_session_id"
      pattern: "_VALID_USER_ID"
---

<objective>
Implement LocalFSVault (T2 canonical vault adapter), wire vault promotion + eviction into ConsolidationPipeline.run() as TWO SEPARATE iteration loops (vault first, eviction second), connect engine.consolidate() to pass user_id through to the pipeline, and add engine_with_vault fixture to conftest.

Purpose: Vault promotion MUST run before eviction (Pitfall 8 is load-bearing). The two-pass requirement is NOT merely ordering-within-a-loop — it requires TWO SEPARATE loops: Loop 1 iterates live_records for promotion, Loop 2 then runs decay_pass for eviction. This is the only guarantee that a record qualifying for both (high salience + low keep_score) ends up in the vault before being retired. The test_vault_promotion_before_eviction test proves this with a borderline record (salience=0.72, old, zero access).
Output: LocalFSVault, consolidation.py vault+eviction wiring with two-loop ordering, engine.consolidate() user_id passthrough, conftest engine_with_vault fixture. CONS-09/TIER-03 tests go GREEN.
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-CONTEXT.md
@.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-RESEARCH.md
@.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-PATTERNS.md
@.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-01-SUMMARY.md
@.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-02-SUMMARY.md
</context>

<interfaces>
From src/mnema/ports/vault.py (created in Plan 00):
  class VaultStore(Protocol):
      async def promote(self, record: MemoryRecord) -> None: ...
      async def get_user_model(self, user_id: str) -> str: ...

From src/mnema/adapters/object_store/local_fs.py — exact patterns to copy for LocalFSVault:
  _VALID_SESSION_ID = re.compile(r"^[A-Za-z0-9_\-]+$")
  def _validate_session_id(session_id: str) -> None:
      if not _VALID_SESSION_ID.match(session_id):
          raise ValueError(...)
  class LocalFS:
      def __init__(self, base_dir: str) -> None:
          self._base = Path(base_dir)
          self._base.mkdir(parents=True, exist_ok=True)
      # path construction: self._base / f"{session_id}.jsonl"
      # write pattern: with path.open("a", encoding="utf-8") as fh: fh.write(...)
      # read pattern: path.read_text(encoding="utf-8") if path.exists() else ""

From src/mnema/core/consolidation.py (current) — the loop to modify:
  class ConsolidationPipeline:
      def __init__(self, *, llm, embedder, record_store, vector_index, staging_queue): ...
      async def run(self) -> None:
          items = self._drain_queue()
          for item in items: await self._process_turn(...)
          from mnema.core.decay import decay_pass  # noqa: PLC0415
          processed_user_ids = {item.get("user_id","") for item in items if item.get("user_id")}
          for uid in processed_user_ids:
              async for _record, _score in decay_pass(self._record_store, uid):
                  pass  # Phase 3 REPLACES this pass with vault+eviction logic

From src/mnema/core/engine.py (after Plan 01):
  KEEP_THRESHOLD: float = 0.3
  class MemoryEngine:
      def __init__(self, *, embedder, t1, t0, scheduler, llm=None, vault=None):
          self._vault = vault
          # ConsolidationPipeline constructed WITHOUT vault kwarg currently — Plan 03 must update this
      async def evict(self, *, user_id: str) -> int: ...  # implemented in Plan 01
      async def consolidate(self, *, force=False) -> None:
          await self._consolidation_pipeline.run()
          await self._scheduler.trigger_now()

From src/mnema/core/schema.py:
  class MemoryRecord:
      id: str; user_id: str; record_type: RecordType; content: str; summary: str
      salience: float; protected: bool; provisional: bool; valid_until: Optional[datetime]
  class RecordType(StrEnum):
      FACT="fact"; PREFERENCE="preference"; EVENT="event"; PROCEDURE="procedure"

ORDERING INVARIANT (load-bearing, Pitfall 8):
  For each uid in processed_user_ids:
    Loop 1 (vault): async for record in record_store.live_records(uid): if qualifying → vault.promote(record)
    Loop 2 (eviction): async for record, score in decay_pass(record_store, uid, now=now): if score < KEEP_THRESHOLD → 4-step eviction
  These are TWO SEPARATE for-loops. A single loop that does both would not guarantee promotion before eviction
  for records that qualify for both (salience >= threshold AND keep_score < KEEP_THRESHOLD).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement LocalFSVault adapter</name>
  <files>src/mnema/adapters/vault/local_fs_vault.py</files>
  <read_first>src/mnema/adapters/object_store/local_fs.py (full — 128 lines), src/mnema/ports/vault.py (VaultStore Protocol), 03-PATTERNS.md "local_fs_vault.py" section, 03-RESEARCH.md Pattern 4 (LocalFSVault code reference)</read_first>
  <behavior>
    LocalFSVault.__init__(base_dir: str):
    - Creates Path(base_dir) with mkdir(parents=True, exist_ok=True)

    LocalFSVault.promote(record: MemoryRecord) -> None:
    - Validates record.user_id against _VALID_USER_ID regex (same pattern as LocalFS._VALID_SESSION_ID: r"^[A-Za-z0-9_\-]+$") — raises ValueError on invalid user_id (path traversal prevention per RESEARCH.md security domain)
    - Reads existing {base_dir}/{user_id}.md (empty string if not exists)
    - summary = (record.summary or record.content[:80]).strip()
    - If summary already in existing content: return (idempotent dedup, D3-12)
    - section_header = "## {record.record_type.value.capitalize()}s"
    - If section_header in existing: insert bullet after header line
    - Else: append new section + bullet to end
    - Writes updated content to {user_id}.md (write_text, not append)

    LocalFSVault.get_user_model(user_id: str) -> str:
    - Validates user_id; returns file content or "" if not exists

    Structural typing: LocalFSVault does NOT inherit from VaultStore (D-08).
  </behavior>
  <action>
    Create src/mnema/adapters/vault/local_fs_vault.py. Module docstring mirrors local_fs.py style: document T2 layout ({base_dir}/{user_id}.md), path traversal security note, I/O note.

    Module-level: import re, Path; define _VALID_USER_ID = re.compile(r"^[A-Za-z0-9_\-]+$") and _validate_user_id(user_id: str) -> None (same structure as _validate_session_id in local_fs.py, raises ValueError on mismatch).

    LocalFSVault class: no inheritance. __init__ creates base dir. _vault_path(user_id) calls _validate_user_id then returns self._base / f"{user_id}.md".

    promote() implementation: validate user_id first. Read existing. Compute summary. Check dedup. Build section header as f"## {record.record_type.value.capitalize()}s". The section insertion logic: if section_header+"\n" in existing, use str.replace to insert bullet after the header; else append "\n{section_header}\n- {summary}\n". Write result to file. Strip leading whitespace from final content before writing (existing.lstrip() or "").

    get_user_model(): validate user_id, read and return file text.

    Import from mnema.core.schema: MemoryRecord is needed at runtime (not just TYPE_CHECKING) because promote() reads record fields. Use direct import: "from mnema.core.schema import MemoryRecord" — this is an adapter, not a port; circular import risk is low since schema has no adapter imports.
  </action>
  <verify>
    <automated>cd C:/Users/HomePC/Documents/GitHub/memoryAgent && uv run --extra dev pytest tests/test_vault.py::TestLocalFSVault -q --tb=short 2>&1 | tail -10</automated>
  </verify>
  <done>TestLocalFSVault tests pass (markdown written with section headers; dedup prevents duplicate bullets; promote is idempotent); _validate_user_id raises ValueError for "../../etc/passwd"; get_user_model returns empty string if no file; pyright strict passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: LocalFSVault conftest fixture + vault test helpers</name>
  <files>tests/conftest.py</files>
  <read_first>tests/conftest.py (full — current fixtures), src/mnema/adapters/vault/local_fs_vault.py (just created in Task 1)</read_first>
  <behavior>
    - engine_with_vault fixture: async fixture that creates a LocalFSVault from tmp_path/"vault", constructs MemoryEngine(..., vault=vault_instance), yields engine, tears down scheduler on exit
    - The fixture mirrors the existing engine fixture structure (same adapters: stub_embedder, SqliteT1, LocalFS, InProcessScheduler) but adds vault= kwarg
    - test_vault.py tests that use engine_with_vault will import it from conftest automatically (pytest fixture injection)
  </behavior>
  <action>
    Edit tests/conftest.py. After the existing engine_with_llm fixture (or at the end of the fixtures section), add:

      @pytest.fixture
      async def engine_with_vault(tmp_path, stub_embedder):
          """Engine fixture with a LocalFSVault wired in for CONS-09/TIER-03 tests."""
          from mnema import MemoryEngine  # noqa: PLC0415
          from mnema.adapters.object_store.local_fs import LocalFS  # noqa: PLC0415
          from mnema.adapters.vector_store.sqlite_t1 import SqliteT1  # noqa: PLC0415
          from mnema.adapters.scheduler.in_process import InProcessScheduler  # noqa: PLC0415
          from mnema.adapters.vault.local_fs_vault import LocalFSVault  # noqa: PLC0415

          vault = LocalFSVault(str(tmp_path / "vault"))
          t1 = SqliteT1(str(tmp_path / "t1.db"), dim=stub_embedder.dim)
          t0 = LocalFS(str(tmp_path / "t0"))
          scheduler = InProcessScheduler()
          eng = MemoryEngine(
              embedder=stub_embedder,
              t1=t1,
              t0=t0,
              scheduler=scheduler,
              vault=vault,
          )
          await scheduler.start()
          yield eng
          await scheduler.shutdown()

    Follow the exact teardown pattern of the existing engine fixture in conftest.py (including any connection cleanup if SqliteT1 has a close() method — check the existing fixture for the pattern).
  </action>
  <verify>
    <automated>cd C:/Users/HomePC/Documents/GitHub/memoryAgent && uv run --extra dev pytest tests/test_vault.py -k "engine_with_vault" --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>engine_with_vault fixture importable from conftest; tests/test_vault.py tests that request engine_with_vault are collected without error; pyright passes on conftest.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: Wire vault+eviction into ConsolidationPipeline + engine.consolidate() user_id passthrough</name>
  <files>src/mnema/core/consolidation.py, src/mnema/core/engine.py</files>
  <read_first>src/mnema/core/consolidation.py (full — 417 lines), src/mnema/core/engine.py (full — 379 lines, updated by Plans 01+02), 03-PATTERNS.md "consolidation.py" section, 03-RESEARCH.md Pitfall 8 (eviction-before-vault ordering)</read_first>
  <behavior>
    ConsolidationPipeline.__init__ gains vault: Any = None and t0: Any = None kwargs; stored as self._vault and self._t0.

    ConsolidationPipeline.run(user_id: str | None = None):
    - When user_id is not None: scope processed_user_ids to {user_id} if it appears in the staged items' user_ids. Process only that user's staged items (filter items list to user_id-matching items before processing turns). This makes consolidate(user_id="u1") process only u1's staged turns and run vault/eviction only for u1.
    - When user_id is None: current global behavior — process all staged items, all processed_user_ids.

    ORDERING (load-bearing — TWO SEPARATE LOOPS per uid, per Pitfall 8):
      For each uid in processed_user_ids:
        LOOP 1 — Vault promotion pass (runs FIRST, BEFORE any eviction):
          If self._vault is not None:
            async for record in self._record_store.live_records(uid):
              if (not record.provisional and record.salience >= VAULT_SALIENCE_THRESHOLD and record.valid_until is None):
                await self._vault.promote(record)
        LOOP 2 — Eviction pass (runs SECOND, AFTER vault loop completes for this uid):
          Import and use decay_pass; replace the "pass" with score-threshold check and 4-step eviction:
            async for record, score in decay_pass(self._record_store, uid, now=now):
              if score < KEEP_THRESHOLD:
                await self._record_store.update(record.id, valid_until=now)
                await self._vector_index.delete_vector(record.id)
                if self._t0 is not None:
                  await self._t0.archive(record)
                  await self._t0.append_audit({eviction entry})

    Add VAULT_SALIENCE_THRESHOLD = 0.7 and KEEP_THRESHOLD = 0.3 at module level in consolidation.py.

    engine.consolidate(force=False, user_id: str | None = None):
    - Add optional user_id: str | None = None parameter.
    - Pass user_id to self._consolidation_pipeline.run(user_id=user_id).
    - Docstring: "When user_id is set, processes only that user's staged turns and runs vault/eviction scoped to that user. When None, processes all staged users (global behavior)."

    engine MemoryEngine.__init__: update ConsolidationPipeline construction to pass vault=self._vault and t0=self._t0.

    test_vault_promotion_before_eviction (GREEN now): the conftest engine_with_vault fixture is available. Seed a record with:
    - salience=0.72 (above VAULT_SALIENCE_THRESHOLD=0.7)
    - created_at backdated 180 days (so keep_score is very low, well below KEEP_THRESHOLD=0.3)
    - access_count=0 (no accesses)
    - provisional=False (confirmed — eligible for vault)
    - valid_until=None (live)
    Then call engine.consolidate(). Assertions:
    1. vault_markdown = vault.get_user_model(user_id) contains the record's summary (PROMOTED)
    2. record_in_t1 = await engine.t1.get(record_id); assert record_in_t1.valid_until is not None (EVICTED after promotion)
    Both assertions must pass — proving vault promotion ran before eviction (if eviction ran first, valid_until would be set BEFORE promote(), but promote() only promotes records with valid_until IS NULL).
  </behavior>
  <action>
    Edit src/mnema/core/consolidation.py:
    1. Add module-level constants VAULT_SALIENCE_THRESHOLD = 0.7 and KEEP_THRESHOLD = 0.3 with docstrings. KEEP_THRESHOLD docstring: "mirrors engine.KEEP_THRESHOLD — tune both together. Defined independently here to avoid circular imports."
    2. Add "t0: Any = None" and "vault: Any = None" as kwargs to __init__. Store as self._t0 and self._vault.
    3. Update run() signature: async def run(self, *, user_id: str | None = None) -> None.
    4. In run(): when user_id is not None, filter items list: items = [i for i in items if i.get("user_id") == user_id]. The processed_user_ids set will then be at most {user_id}.
    5. Replace the existing for-uid loop body with TWO SEPARATE LOOPS per uid:
       LOOP 1 (vault): if self._vault is not None: async for record in self._record_store.live_records(uid): promote qualifying records.
       LOOP 2 (eviction): async for record, score in decay_pass(self._record_store, uid, now=now): if score < KEEP_THRESHOLD: 4-step eviction (guard self._t0 is not None before archive/audit calls — backward compatibility for tests that don't pass t0).
    6. Import decay_pass at method body level (existing pattern). Import datetime/timezone at method body level.
    7. The two loops must not be merged — they MUST be separate for-loops under the per-uid block.

    Edit src/mnema/core/engine.py:
    1. Add "user_id: str | None = None" to engine.consolidate() signature. Update call to: await self._consolidation_pipeline.run(user_id=user_id).
    2. Update ConsolidationPipeline construction to pass vault=self._vault and t0=self._t0 (two new kwargs). This is in __init__ where the pipeline is lazy-constructed.
    3. Update engine.consolidate() docstring to document user_id parameter and scoped behavior.
    4. ScopedHandle does NOT get a consolidate() method — consolidation is user-scoped at the engine level and the MCP tool passes user_id explicitly (D3-14).
  </action>
  <verify>
    <automated>cd C:/Users/HomePC/Documents/GitHub/memoryAgent && uv run --extra dev pytest tests/test_vault.py -q --tb=short 2>&1 | tail -15</automated>
  </verify>
  <done>tests/test_vault.py all pass: CONS-09 promotion test (after consolidate(), high-salience record appears in vault markdown); TIER-03 format tests pass; test_vault_promotion_before_eviction passes (salience=0.72 record is both in vault AND has valid_until set, proving vault-first ordering); VAULT_SALIENCE_THRESHOLD = 0.7 in consolidation.py; full suite remains GREEN; pyright strict passes on consolidation.py and engine.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user_id → vault filesystem path | user_id is used directly as filename component |
| consolidation pipeline → vault promote → filesystem | T2 markdown files are written during consolidation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | LocalFSVault path traversal via user_id | mitigate | _validate_user_id() raises ValueError for any user_id not matching r"^[A-Za-z0-9_\-]+$" — mirrors LocalFS._validate_session_id() pattern (T-1-06); tested in TestLocalFSVault with "../../etc/passwd" input |
| T-03-03-02 | Tampering | Vault write race condition | accept | MVP runs single-process InProcessScheduler; asyncio is single-threaded; concurrent consolidations impossible. Flag for Phase 4 when moving to cloud scheduler. (RESEARCH.md Pitfall 4) |
| T-03-03-03 | Tampering | ConsolidationPipeline eviction + vault ordering | mitigate | Vault promotion runs in a SEPARATE LOOP BEFORE the decay_pass eviction loop (Pitfall 8). test_vault_promotion_before_eviction proves: a record with salience=0.72 (above threshold) + low keep_score ends up in vault AND has valid_until set — vault-then-evict, never evict-then-vault. |
| T-03-03-04 | Information Disclosure | T2 vault markdown file contents | accept | MVP vault files are local plaintext; no user authentication at file level. Phase 4 will move vault to encrypted or access-controlled storage if needed. |
| T-03-03-05 | Tampering | consolidate(user_id="u1") isolation | mitigate | ConsolidationPipeline.run(user_id="u1") filters staged items to user_id="u1" and scopes vault/eviction to uid="u1"; tested by test_consolidate_user_isolation (add to test_vault.py if not already present — assert consolidate(user_id="u1") does not process u2's staged turns). |
</threat_model>

<verification>
- uv run --extra dev pytest tests/test_vault.py -q exits 0
- test_promotion_on_consolidation: confirmed high-salience record appears in {tmp_path}/vault/{user_id}.md
- test_vault_promotion_before_eviction: salience=0.72 record is in vault AND valid_until is set (ordering proven)
- LocalFSVault._validate_user_id("../../etc/passwd") raises ValueError
- Vault markdown contains "## Facts" or appropriate section header
- Promote idempotency: calling promote() twice on same record produces one bullet, not two
- ConsolidationPipeline run() has TWO SEPARATE LOOPS per uid (vault loop, then eviction loop)
- engine.consolidate(user_id="u1") only processes u1's staged turns
- uv run --extra dev pytest tests/ -q: all 33+ tests GREEN
- uv run --extra dev pyright: exits 0
</verification>

<success_criteria>
- tests/test_vault.py: all tests GREEN including test_vault_promotion_before_eviction
- LocalFSVault: writes {user_id}.md, sectioned, deduped, path-traversal validated
- ConsolidationPipeline.run(): vault loop (Loop 1) and eviction loop (Loop 2) are separate — never merged
- engine_with_vault fixture in conftest.py
- VAULT_SALIENCE_THRESHOLD = 0.7 in consolidation.py
- engine.consolidate(user_id=...) accepted and passed through to pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-03-SUMMARY.md`
</output>
