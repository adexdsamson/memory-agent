---
phase: 03-forgetting-salience-floor-budget-packer-mcp
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - src/mnema/ports/vault.py
  - src/mnema/mcp/__init__.py
  - src/mnema/adapters/vault/__init__.py
  - tests/test_forgetting.py
  - tests/test_recall_packer.py
  - tests/test_vault.py
  - tests/test_mcp_server.py
autonomous: true
requirements:
  - FORG-02
  - FORG-03
  - FORG-04
  - RECALL-03
  - RECALL-04
  - RECALL-05
  - CONS-09
  - TIER-03
  - IFACE-02

must_haves:
  truths:
    - "pytest --collect-only exits 0 with 4 new test files visible (RED: tests fail, not error)"
    - "pyproject.toml has fastmcp, tiktoken, hypothesis dep additions committed"
    - "VaultStore Protocol file exists with promote() and get_user_model() stubs"
    - "All 9 Phase 3 requirement IDs have at least one test function covering them"
  artifacts:
    - path: "pyproject.toml"
      provides: "fastmcp>=3.4.2,<4 in [project.dependencies]; tiktoken>=0.13 in [project.dependencies]; hypothesis>=6.155 in [project.optional-dependencies.dev]"
      contains: "fastmcp"
    - path: "src/mnema/ports/vault.py"
      provides: "VaultStore Protocol — 6th adapter axis"
      exports: ["VaultStore"]
    - path: "tests/test_forgetting.py"
      provides: "RED stubs covering FORG-02/03/04"
      min_lines: 30
    - path: "tests/test_recall_packer.py"
      provides: "RED stubs covering RECALL-03/04/05"
      min_lines: 30
    - path: "tests/test_vault.py"
      provides: "RED stubs covering CONS-09/TIER-03"
      min_lines: 20
    - path: "tests/test_mcp_server.py"
      provides: "RED stubs covering IFACE-02"
      min_lines: 20
  key_links:
    - from: "tests/test_forgetting.py"
      to: "mnema.core.engine"
      via: "deferred import inside test body (noqa: PLC0415)"
      pattern: "from mnema"
    - from: "src/mnema/ports/vault.py"
      to: "mnema.core.schema.MemoryRecord"
      via: "import"
      pattern: "MemoryRecord"
---

<objective>
Wave 0: add three runtime/dev deps, create the VaultStore Protocol (6th adapter axis), new package inits, and four RED test files that define what Phase 3 must prove.

Purpose: Executors in subsequent waves need importable test targets and the VaultStore Protocol before they can write implementation code. Deps must be pinned before any import of fastmcp or hypothesis.
Output: pyproject.toml updated; VaultStore Protocol; four test files in RED (collected, failing at import/fixture, not erroring at collection time); two package __init__.py files.
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-CONTEXT.md
@.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-RESEARCH.md
@.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-PATTERNS.md
@.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-VALIDATION.md
</context>

<interfaces>
From src/mnema/ports/scheduler.py — exact Protocol shape to copy for VaultStore:
  class Scheduler(Protocol):
      async def schedule(self, fn: object, *, every_seconds: int) -> None: ...
      async def trigger_now(self) -> None: ...
      async def start(self) -> None: ...
      async def shutdown(self) -> None: ...

From src/mnema/ports/object_store.py — import pattern for MemoryRecord in a port file:
  from mnema.core.schema import MemoryRecord, Turn
  class ObjectStorePort(Protocol):
      async def archive(self, record: MemoryRecord) -> str: ...

From src/mnema/core/schema.py:
  class RecordType(StrEnum):
      FACT = "fact"; PREFERENCE = "preference"; EVENT = "event"; PROCEDURE = "procedure"
  class MemoryRecord(BaseModel):
      id: str; user_id: str; session_id: str; record_type: RecordType
      content: str; summary: str; protected: bool; salience: float
      provisional: bool; valid_until: Optional[datetime]

From tests/conftest.py — engine fixture pattern (deferred imports, tmp_path):
  @pytest.fixture
  async def engine(tmp_path, stub_embedder):
      from mnema import MemoryEngine  # noqa: PLC0415
      from mnema.adapters.object_store.local_fs import LocalFS  # noqa: PLC0415
      ...
      yield eng
      await scheduler.shutdown()

From tests/test_decay.py — asyncio.run() inside sync Hypothesis test (FORG-03 pattern):
  def test_protected_records_never_evicted():  # sync, not async
      async def _collect():
          ...
      result = asyncio.run(_collect())
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 3 deps to pyproject.toml + create package inits</name>
  <files>pyproject.toml, src/mnema/mcp/__init__.py, src/mnema/adapters/vault/__init__.py</files>
  <read_first>pyproject.toml (current dep list), src/mnema/adapters/__init__.py (blank init pattern)</read_first>
  <action>
    Edit pyproject.toml:
    - In [project.dependencies], add "fastmcp>=3.4.2,<4" and "tiktoken>=0.13" (both runtime — tiktoken is used by TiktokenCounter at engine runtime).
    - In [project.optional-dependencies] dev, add "hypothesis>=6.155".
    These versions are confirmed from RESEARCH.md (fastmcp 3.4.2, hypothesis 6.155.2, tiktoken 0.13.0 verified against PyPI 2026-06-14).

    Create src/mnema/mcp/__init__.py as an empty module docstring file: '"""MNEMA MCP server surface."""'.
    Create src/mnema/adapters/vault/__init__.py as an empty module docstring file: '"""MNEMA vault adapter implementations."""'.
  </action>
  <verify>
    <automated>cd C:/Users/HomePC/Documents/GitHub/memoryAgent && uv run --extra dev python -c "import tomllib; d=tomllib.loads(open('pyproject.toml').read()); deps=' '.join(d['project']['dependencies']); assert 'fastmcp' in deps, 'missing fastmcp'; assert 'tiktoken' in deps, 'missing tiktoken'; dev=' '.join(d['project']['optional-dependencies']['dev']); assert 'hypothesis' in dev, 'missing hypothesis'; print('pyproject OK')"</automated>
  </verify>
  <acceptance_criteria>pyproject.toml contains fastmcp>=3.4.2,<4 and tiktoken>=0.13 in [project.dependencies] and hypothesis>=6.155 in dev; both __init__.py files exist.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create VaultStore Protocol (6th adapter axis)</name>
  <files>src/mnema/ports/vault.py</files>
  <read_first>src/mnema/ports/scheduler.py (exact Protocol shape), src/mnema/ports/object_store.py (MemoryRecord import pattern), 03-PATTERNS.md VaultStore section</read_first>
  <action>
    Create src/mnema/ports/vault.py. Module docstring: "VaultStore port — TIER-03 / CONS-09. The 6th adapter axis: T2 canonical vault for merged, deduped, human-readable, git-versioned user model. Per D3-09/D-10: no @runtime_checkable — static checking only."

    Imports: "from __future__ import annotations", "from typing import Protocol", TYPE_CHECKING guard for MemoryRecord.

    VaultStore(Protocol) with two async methods:
    - promote(record: MemoryRecord) -> None — docstring: "Promote a confirmed, stable record into the T2 canonical vault. Implementations must dedup by content/summary (D3-12), write human-readable git-versioned format (TIER-03), and be idempotent."
    - get_user_model(user_id: str) -> str — docstring: "Return the current T2 user model as a string."

    Follow the scheduler.py Protocol shape exactly: no @runtime_checkable, body is ellipsis (...), async methods per D-11. MemoryRecord imported under TYPE_CHECKING only.
  </action>
  <verify>
    <automated>cd C:/Users/HomePC/Documents/GitHub/memoryAgent && uv run --extra dev python -c "from mnema.ports.vault import VaultStore; import inspect; methods=set(k for k,v in inspect.getmembers(VaultStore) if not k.startswith('_')); assert 'promote' in methods and 'get_user_model' in methods, f'missing methods: {methods}'; print('VaultStore Protocol OK')"</automated>
  </verify>
  <acceptance_criteria>src/mnema/ports/vault.py exists; VaultStore is a Protocol with promote(record) and get_user_model(user_id) async methods; no @runtime_checkable; pyright strict passes on this file.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create four RED test stub files (FORG/RECALL/VAULT/MCP)</name>
  <files>tests/test_forgetting.py, tests/test_recall_packer.py, tests/test_vault.py, tests/test_mcp_server.py</files>
  <read_first>tests/conftest.py (engine fixture), tests/test_decay.py (Hypothesis asyncio.run() pattern + MockStore pattern + TestDecay class style), 03-VALIDATION.md (per-requirement test map)</read_first>
  <behavior>
    tests/test_forgetting.py:
    - TestEviction class with: test_eviction_sets_valid_until (FORG-02), test_eviction_archives_to_cold_store (FORG-02), test_eviction_audit_jsonl (FORG-04), test_eviction_skips_protected (FORG-03 example supplement)
    - Standalone Hypothesis property test: test_protected_records_never_evicted (FORG-03) — sync def, uses asyncio.run() per RESEARCH.md Pattern 2

    tests/test_recall_packer.py:
    - TestReRank class: test_rerank_order_by_composite_score (RECALL-03)
    - TestPacker class: test_pack_under_budget (RECALL-04), test_pack_respects_budget_limit (RECALL-04)
    - Standalone: test_critical_fact_survives_large_off_topic_history (RECALL-05 adversarial)

    tests/test_vault.py:
    - TestLocalFSVault class: test_promote_writes_markdown (TIER-03), test_promote_deduplication (TIER-03), test_promote_sectioned_by_type (TIER-03)
    - Standalone: test_promotion_on_consolidation (CONS-09) — uses engine_with_vault fixture (defined in this file or conftest)

    tests/test_mcp_server.py:
    - test_mcp_tools_list (IFACE-02) — async, uses Client(mcp_server)
    - test_mcp_remember_recall_roundtrip (IFACE-02) — async, uses Client(mcp_server)
    - mcp_server fixture defined in this file
  </behavior>
  <action>
    Write all four test files as RED stubs — all test functions exist and are collected, but they import from not-yet-implemented modules using deferred imports inside the test body (noqa: PLC0415 per PATTERNS.md Deferred Imports pattern). Use pytest.raises(ImportError) or bare assert False with a TODO comment as the failure mode so tests FAIL (not error-out at collection time).

    For test_forgetting.py: the Hypothesis test must be def (sync), not async def, per RESEARCH.md Pitfall 1. Use @given(record_set_strategy()) with @settings(max_examples=100). The record_set_strategy uses @st.composite with st.integers, st.booleans, st.floats, st.text, st.sampled_from(list(RecordType)). The test body calls asyncio.run(_collect()). In Wave 0, _collect imports from mnema.core.decay which exists — so the Hypothesis test can be more complete (it tests the structural guarantee that already holds in decay_pass). Mark the integration tests that need engine.evict() as xfail or simply let them fail at the deferred import step.

    For test_mcp_server.py: the mcp_server fixture calls "from mnema.mcp.server import create_mcp_server" (not yet implemented) — this is fine; pytest collects the test but the fixture fails at execution time in RED state.

    For test_vault.py: engine_with_vault fixture imports LocalFSVault (not yet implemented) — same pattern.

    Key requirement: `uv run --extra dev pytest --collect-only` must exit 0 (no collection errors, only execution errors when tests run).
  </action>
  <verify>
    <automated>cd C:/Users/HomePC/Documents/GitHub/memoryAgent && uv run --extra dev pytest --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>pytest --collect-only exits 0; tests/test_forgetting.py, test_recall_packer.py, test_vault.py, test_mcp_server.py all appear in collected output; no ERROR (collection errors) — only expected FAILED tests when run; full suite still passes for Phase 1+2 tests.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → source imports | Deferred imports in test stubs; RED state means import failures, not security vectors |
| pyproject.toml dep pinning | Version constraints prevent pulling incompatible pre-release packages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-00-01 | Tampering | pyproject.toml dep bounds | mitigate | Pin fastmcp<4 to prevent breaking API changes from 4.x pre-releases pulling in automatically |
| T-03-00-02 | Tampering | VaultStore Protocol | accept | Protocol-only file; no runtime behavior; static checking enforces contract at compile time |
</threat_model>

<verification>
- pyproject.toml: fastmcp, tiktoken, hypothesis present in correct dep sections
- pytest --collect-only exits 0 with all four new test files visible
- VaultStore Protocol: two async methods, no @runtime_checkable, pyright passes
- Existing Phase 1+2 tests (33 tests) remain GREEN
</verification>

<success_criteria>
- `uv run --extra dev pytest --collect-only -q` exits 0 with test_forgetting.py, test_recall_packer.py, test_vault.py, test_mcp_server.py in output
- `uv run --extra dev pytest tests/ -q` shows Phase 1+2 tests still GREEN, new Phase 3 tests FAILED (not ERROR)
- pyproject.toml contains all three new dep lines
- src/mnema/ports/vault.py exists with VaultStore Protocol
</success_criteria>

<output>
After completion, create `.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-00-SUMMARY.md`
</output>
