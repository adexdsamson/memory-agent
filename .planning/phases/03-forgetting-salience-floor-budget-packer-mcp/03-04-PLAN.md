---
phase: 03-forgetting-salience-floor-budget-packer-mcp
plan: "04"
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - src/mnema/mcp/server.py
  - tests/test_mcp_server.py
autonomous: true
requirements:
  - IFACE-02

must_haves:
  truths:
    - "FastMCP server exposes exactly five tools: remember, recall, forget, consolidate, expand"
    - "user_id is an explicit, required argument on every tool — no defaulted or ambient scope"
    - "MCP tools delegate all business logic to the injected MemoryEngine — no logic duplication in tool layer"
    - "In-process client tests (Client(server)) pass without any transport overhead"
    - "remember→recall roundtrip via MCP tool surface returns the stored content"
    - "Full Phase 3 test suite (all requirements FORG-02/03/04, RECALL-03/04/05, CONS-09, TIER-03, IFACE-02) is GREEN"
    - "pyright strict passes on all Phase 3 source files"
  artifacts:
    - path: "src/mnema/mcp/server.py"
      provides: "create_mcp_server(engine) -> FastMCP factory; five @mcp.tool async functions; __main__ stdio entry point"
      exports: ["create_mcp_server"]
      contains: "create_mcp_server"
  key_links:
    - from: "src/mnema/mcp/server.py create_mcp_server"
      to: "mnema.core.engine.MemoryEngine"
      via: "closure capture — engine passed into factory, closed over by nested @mcp.tool functions"
      pattern: "engine\\."
    - from: "tests/test_mcp_server.py"
      to: "fastmcp.Client(mcp_server)"
      via: "async with Client(mcp_server) as client — in-process, no transport (D3-16)"
      pattern: "Client"
---

<objective>
Implement the FastMCP MCP server as a thin wrapper over MemoryEngine, verify all five tools with in-process client tests, then run the full Phase 3 suite and pyright to confirm phase gate.

Purpose: The MCP server is the final integration surface — it proves that the same Python functions exposed by the SDK are also accessible via the MCP protocol (IFACE-02 thesis). No business logic lives in the tool layer.
Output: src/mnema/mcp/server.py, GREEN tests/test_mcp_server.py, full Phase 3 suite GREEN, pyright clean.
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-CONTEXT.md
@.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-RESEARCH.md
@.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-PATTERNS.md
@.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-03-SUMMARY.md
</context>

<interfaces>
From src/mnema/core/engine.py (after Plans 01-03):
  class MemoryEngine:
      async def remember(self, content: str, *, user_id: str, session_id: str,
                         agent_id=None, type_hint=None, durable=False) -> str: ...
      async def recall(self, query: str, *, user_id: str, agent_id=None, k=30,
                       budget: int | None = None) -> list[MemoryRecord]: ...
      async def expand(self, record_id: str, *, user_id: str) -> Optional[Turn]: ...
      async def forget(self, record_id: str, *, user_id: str, reason: str = "") -> None: ...
      async def consolidate(self, *, force: bool = False) -> None: ...

From src/mnema/core/schema.py:
  class MemoryRecord(BaseModel):
      id: str; summary: str; record_type: RecordType; salience: float; protected: bool
  class Turn(BaseModel):
      content: str; role: str; created_at: datetime

FastMCP 3.4.2 API (verified 2026-06-14):
  from fastmcp import FastMCP, Client
  mcp = FastMCP("name")
  @mcp.tool
  async def tool_fn(arg: str) -> str: ...
  # In-process test:
  async with Client(mcp) as client:
      tools = await client.list_tools()   # list[Tool] — each has .name
      result = await client.call_tool("name", {"arg": "value"})
      result.data  # the return value

CRITICAL PITFALLS (RESEARCH.md):
  - Pitfall 1: @given cannot decorate async def — Hypothesis tests are sync (already handled in test_forgetting.py)
  - Pitfall 6: Do NOT call asyncio.run() inside async def tests — use async with Client(server) directly
  - D3-13: closure-capture injection (factory function, not lifespan) — engine passed in, closed over
  - D3-14: user_id is explicit, required, non-defaulted on every tool
  - D3-15: stdio transport for production; in-process client for tests
  - D3-16: Tests use Client(server) — no subprocess, no transport
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement MCP server — create_mcp_server factory + five tools</name>
  <files>src/mnema/mcp/server.py</files>
  <read_first>03-RESEARCH.md Pattern 5 (complete MCP server reference implementation lines 657–738), 03-PATTERNS.md "mcp/server.py" section, 03-CONTEXT.md decisions D3-13/14/15/16</read_first>
  <behavior>
    create_mcp_server(engine) -> FastMCP:
    - Creates mcp = FastMCP("mnema")
    - Defines 5 @mcp.tool async functions as closures over engine
    - Returns mcp

    remember(content: str, user_id: str, session_id: str, type_hint: str | None = None, durable: bool = False) -> str:
    - Delegates: return await engine.remember(content, user_id=user_id, session_id=session_id, type_hint=type_hint, durable=durable)

    recall(query: str, user_id: str, k: int = 10, budget: int = 2000) -> list[dict]:
    - Delegates: records = await engine.recall(query, user_id=user_id, k=k, budget=budget)
    - Returns: [{"id": r.id, "summary": r.summary, "record_type": r.record_type.value, "salience": r.salience, "protected": r.protected} for r in records]

    forget(record_id: str, user_id: str, reason: str = "") -> None:
    - Delegates: await engine.forget(record_id, user_id=user_id, reason=reason)
    - Note: engine.forget raises ValueError for protected records or cross-user — let FastMCP propagate the error to the client

    consolidate(user_id: str) -> str:
    - Delegates: await engine.consolidate()
    - Returns: "consolidated"
    - Note: user_id is required for the explicit-arg contract (D3-14) even though engine.consolidate() doesn't take user_id currently — accept it but don't pass it (document this)

    expand(record_id: str, user_id: str) -> dict | None:
    - Delegates: turn = await engine.expand(record_id, user_id=user_id)
    - Returns None if turn is None
    - Returns: {"content": turn.content, "role": turn.role, "created_at": turn.created_at.isoformat()}

    __main__ block:
    - Creates a minimal engine from local adapters and runs the server via mcp_server.run() (stdio default)
    - Wrapped in if __name__ == "__main__" guard
  </behavior>
  <action>
    Create src/mnema/mcp/server.py. Module docstring: "MNEMA MCP server — thin FastMCP wrapper over MemoryEngine (IFACE-02). All business logic lives in the engine. This file contains ONLY protocol plumbing: tool registration, arg extraction, response serialization. Per D3-13: closure-capture injection. Per D3-14: user_id is explicit and required on every tool."

    Imports: "from __future__ import annotations", "from typing import Any", "from fastmcp import FastMCP". TYPE_CHECKING import of MemoryEngine.

    create_mcp_server(engine: "MemoryEngine") -> FastMCP: factory function. mcp = FastMCP("mnema"). Define all 5 tools as inner async functions with @mcp.tool decorator. Return mcp.

    The tool functions close over engine via Python closure. Each tool's function signature uses plain Python types (str, int, bool) so FastMCP can auto-generate the Pydantic schema. No Pydantic models needed for tool args (FastMCP handles schema gen from type hints).

    __main__ block uses deferred imports to construct a minimal engine (StubEmbedder, SqliteT1, LocalFS, InProcessScheduler) for local stdio testing. The block is clearly marked as "dev/smoke test only" in a comment. The block calls asyncio.run() around async adapter construction, then calls mcp_server.run() synchronously (FastMCP 3.x .run() is the stdlib-stdio entry point).
  </action>
  <verify>
    <automated>cd C:/Users/HomePC/Documents/GitHub/memoryAgent && uv run --extra dev python -c "from mnema.mcp.server import create_mcp_server; print('server importable OK')"</automated>
  </verify>
  <acceptance_criteria>create_mcp_server() is importable; FastMCP instance created; pyright strict passes on server.py.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP tests GREEN + Phase 3 full suite phase gate</name>
  <files>tests/test_mcp_server.py</files>
  <read_first>tests/test_mcp_server.py (current RED stubs from Plan 00), tests/conftest.py (engine fixture), 03-RESEARCH.md Pattern 5 in-process test section (lines 922–950), 03-RESEARCH.md Pitfall 6 (no asyncio.run in async tests)</read_first>
  <behavior>
    test_mcp_tools_list(mcp_server): async, uses async with Client(mcp_server) as client: tools = await client.list_tools(); assert {"remember", "recall", "forget", "consolidate", "expand"} <= {t.name for t in tools}

    test_mcp_remember_recall_roundtrip(mcp_server): async, remembers "I am allergic to peanuts" then recalls "food allergies", asserts at least one result.data item has "peanut" in its summary

    test_mcp_forget_protected_raises(mcp_server): async, remembers content with durable=True type_hint="fact" and protected=True somehow — actually test that calling forget on a non-protected record works, and that forget on a protected record raises via MCP (FastMCP propagates ValueError as a tool error)

    test_mcp_expand_returns_none_for_wrong_user(mcp_server): async, calls expand with wrong user_id, expects None result (engine.expand scope check)

    mcp_server fixture: defined in this file, takes engine fixture, returns create_mcp_server(engine)
  </behavior>
  <action>
    Update tests/test_mcp_server.py to fully implement the tests (replacing RED stubs with GREEN implementations). Use "async with Client(mcp_server) as client:" — NOT asyncio.run() inside async tests (Pitfall 6).

    Add mcp_server fixture at top of file:
      @pytest.fixture
      async def mcp_server(engine):
          from mnema.mcp.server import create_mcp_server  # noqa: PLC0415
          return create_mcp_server(engine)

    Implement all test functions using the async Client pattern. For test_mcp_remember_recall_roundtrip: call_tool("remember", {...}) then call_tool("recall", {"query": "food allergies", "user_id": "u1", "budget": 2000}); result.data is a list of dicts; assert any "peanut" in item["summary"].lower() for item in result.data.

    For user_id isolation test: remember with user_id="u1", then expand with user_id="u2" — assert result.data is None (engine scope check propagates through MCP layer).

    After implementing tests, also run the full Phase 3 + Phase 1+2 suite to confirm phase gate. The phase gate acceptance criteria below captures this.
  </action>
  <verify>
    <automated>cd C:/Users/HomePC/Documents/GitHub/memoryAgent && uv run --extra dev pytest tests/ -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>All tests in tests/test_mcp_server.py GREEN; full suite (all phases) GREEN with zero failures; "uv run --extra dev pyright" exits 0 (strict mode on src/mnema/); no test uses asyncio.run() inside async def test body.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → MCP tool layer | user_id crosses this boundary as an explicit tool argument |
| MCP tool layer → MemoryEngine | tool functions delegate immediately; no business logic in tool layer |
| MCP server (stdio) → caller process | stdio transport is local-only; no network exposure in MVP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | MCP user_id isolation — tool call for user A must not read/mutate user B | mitigate | user_id is explicit, required, non-defaulted on every tool (D3-14). Engine enforces scope isolation on every read/write (D-02/D-03). Test: expand called with wrong user_id returns None (scope check proven). |
| T-03-04-02 | Tampering | MCP forget — caller evicts another user's record by guessing record_id | mitigate | engine.forget() fetches record and verifies record.user_id == caller's user_id; raises ValueError on mismatch. FastMCP propagates error as tool error to client. |
| T-03-04-03 | Tampering | MCP remember content injection — content field contains MCP-framing characters | accept | FastMCP validates tool args with Pydantic; content is typed str; stored as parameterized SQL in T1 (no interpolation). No new surface. |
| T-03-04-04 | Tampering | MCP recall budget manipulation — caller sets budget=0 to get empty results | accept | Empty packed result is a valid (if unhelpful) response; caller controls their own recall budget. No data leakage possible from a filtered result. |
| T-03-04-05 | Repudiation | No authentication on stdio MCP tools | accept | MVP: stdio is local-only; explicit user_id arg provides identity per call. HTTP/SSE transport + auth → post-MVP (deferred per 03-CONTEXT.md). Document this limitation. |
</threat_model>

<verification>
- uv run --extra dev pytest tests/test_mcp_server.py -q exits 0
- uv run --extra dev pytest tests/ -q exits 0 (full suite: Phase 1+2+3 all GREEN)
- uv run --extra dev pyright exits 0 (strict mode on src/mnema/)
- MCP tool listing returns exactly: {remember, recall, forget, consolidate, expand}
- remember→recall roundtrip via MCP confirms stored fact is retrievable
- expand with wrong user_id returns None (user isolation proven through MCP layer)
- No asyncio.run() inside async def test bodies in test_mcp_server.py
</verification>

<success_criteria>
- tests/test_mcp_server.py: all tests GREEN (IFACE-02)
- Full test suite: all Phase 1+2+3 tests GREEN (9 req IDs all covered)
- pyright strict: exits 0 on src/mnema/
- MCP server: thin wrapper confirmed (no business logic in server.py beyond arg plumbing)
- Phase gate passed: ready for /gsd-verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/03-forgetting-salience-floor-budget-packer-mcp/03-04-SUMMARY.md`
</output>
