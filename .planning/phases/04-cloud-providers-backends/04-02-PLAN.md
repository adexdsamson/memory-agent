---
phase: 04-cloud-providers-backends
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/mnema/adapters/llm/anthropic.py
  - src/mnema/adapters/llm/qwen.py
autonomous: true
requirements:
  - PROV-03
  - PROV-04

must_haves:
  truths:
    - "AnthropicLLM.complete() calls the Anthropic API via asyncio.to_thread (D-13) and returns a non-empty string"
    - "QwenLLM.complete() calls DashScope Generation.call() via asyncio.to_thread and returns a non-empty string"
    - "Both adapters satisfy LLMProvider Protocol by structural typing — no Protocol inheritance"
    - "Both adapters live behind the cloud optional extra; importing them without cloud installed raises ImportError, not AttributeError"
    - "The conformance llm_backend fixture anthropic/qwen parameters skip cleanly without MNEMA_TEST_ANTHROPIC/MNEMA_TEST_DASHSCOPE env vars; turn GREEN when those env vars are set"
  artifacts:
    - path: "src/mnema/adapters/llm/anthropic.py"
      provides: "AnthropicLLM adapter"
      exports: ["AnthropicLLM"]
    - path: "src/mnema/adapters/llm/qwen.py"
      provides: "QwenLLM adapter"
      exports: ["QwenLLM"]
  key_links:
    - from: "AnthropicLLM.complete()"
      to: "anthropic.Anthropic.messages.create()"
      via: "asyncio.to_thread(_call)"
      pattern: "asyncio.to_thread"
    - from: "QwenLLM.complete()"
      to: "dashscope.Generation.call()"
      via: "asyncio.to_thread(_call)"
      pattern: "asyncio.to_thread"
---

<objective>
Implement AnthropicLLM and QwenLLM — the two real LLM adapters that satisfy LLMProvider Protocol via structural typing. Both wrap their sync SDKs in asyncio.to_thread per D-13.

Purpose: PROV-04 (Anthropic adapter) and PROV-03 (Qwen LLM adapter) deliver real reasoning and extraction capability for the cloud config. The conformance llm_backend fixture's anthropic and qwen parameters will turn from skip to pass when MNEMA_TEST_ANTHROPIC/MNEMA_TEST_DASHSCOPE are set.
Output: src/mnema/adapters/llm/anthropic.py (AnthropicLLM), src/mnema/adapters/llm/qwen.py (QwenLLM)
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-cloud-providers-backends/04-CONTEXT.md
@.planning/phases/04-cloud-providers-backends/04-RESEARCH.md
@.planning/phases/04-cloud-providers-backends/04-PATTERNS.md
@.planning/phases/04-cloud-providers-backends/04-01-SUMMARY.md

<interfaces>
<!-- Analog: src/mnema/adapters/llm/stub.py -->
Protocol contract (src/mnema/ports/llm.py):
  class LLMProvider(Protocol):
    async def complete(self, prompt: str, *, model: str | None = None) -> str: ...

StubLLM class shape (analog):
  class StubLLM:
    version: str = "stub-v1"
    def __init__(self) -> None: ...
    async def complete(self, prompt: str, *, model: str | None = None) -> str: ...
  (no Protocol inheritance — structural typing only)

AnthropicLLM SDK call (04-RESEARCH.md Pattern 1):
  from anthropic import Anthropic   # sync client (D-13 pattern)
  client = Anthropic(api_key=api_key)
  resp = client.messages.create(model=m, max_tokens=1024, messages=[{"role": "user", "content": prompt}])
  return resp.content[0].text
  (AsyncAnthropic is also available; use async client if to_thread causes test event-loop issues)

QwenLLM SDK call (04-RESEARCH.md Pattern 2, ASSUMED — verify at install):
  import dashscope
  dashscope.api_key = api_key  # global state — Pitfall 4; document in docstring
  resp = dashscope.Generation.call(model=m, messages=[{"role": "user", "content": prompt}], result_format="message")
  return resp.output.choices[0].message.content
  (If Generation.call() shape differs after install, adapt; the asyncio.to_thread wrapper stays)

Shared patterns (04-PATTERNS.md):
  - from __future__ import annotations at top
  - Structural-typing docstring: "Satisfies LLMProvider Protocol by structural typing."
  - asyncio.to_thread(_call) pattern for all sync SDK leaf calls
  - Lazy import of dashscope inside __init__ body (never at module top — Pitfall 4)
  - Deferred import pattern: import dashscope inside the module file (lazy at module level is fine; the important thing is NOT importing it in src/mnema/__init__.py)

Retry note (04-RESEARCH.md Don't Hand-Roll):
  anthropic SDK has built-in retry — accept SDK defaults.
  dashscope: add basic error handling (check resp.status_code == HTTPStatus.OK or resp.output is not None); raise ValueError with the error message on failure.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AnthropicLLM adapter (PROV-04)</name>
  <files>src/mnema/adapters/llm/anthropic.py</files>
  <behavior>
    - AnthropicLLM(api_key="test", default_model="claude-haiku-4-5") — constructor does not raise
    - hasattr(AnthropicLLM(...), "complete") is True
    - AnthropicLLM satisfies LLMProvider Protocol shape: async complete(prompt, *, model=None) -> str
    - model property returns self._default_model
    - LLM conformance test (test_llm_contract.py) with anthropic param passes when MNEMA_TEST_ANTHROPIC=1 and ANTHROPIC_API_KEY set
  </behavior>
  <read_first>
    src/mnema/adapters/llm/stub.py — copy class shape, __init__ signature, docstring convention, from __future__ import annotations pattern
    04-RESEARCH.md Pattern 1 (lines ~249-280) — AsyncAnthropic / Anthropic call shape
    04-PATTERNS.md AnthropicLLM section (lines ~32-78) — complete() asyncio.to_thread pattern
    src/mnema/ports/llm.py — Protocol contract to satisfy
  </read_first>
  <action>
    Create src/mnema/adapters/llm/anthropic.py:

    Module docstring: "AnthropicLLM — Anthropic Claude LLM adapter. Satisfies LLMProvider Protocol by structural typing. Direct anthropic SDK (D4-05) — no LiteLLM. Sync client wrapped in asyncio.to_thread (D-13). API key from config/env — never hardcoded."

    Imports: from __future__ import annotations; import asyncio; at the bottom of imports, lazy-import anthropic inside __init__ body.

    Class AnthropicLLM (no Protocol inheritance per D-08):
      Docstring noting structural typing and that it implements async complete(prompt, *, model=None) -> str.
      __init__(self, api_key: str, default_model: str = "claude-haiku-4-5") -> None:
        from anthropic import Anthropic  # noqa: PLC0415 — inside __init__ so cloud extra is required only when class is instantiated
        self._client = Anthropic(api_key=api_key)
        self._default_model = default_model

      model property -> str: return self._default_model

      async complete(self, prompt: str, *, model: str | None = None) -> str:
        m = model or self._default_model
        Define _call() -> str closure:
          resp = self._client.messages.create(model=m, max_tokens=1024, messages=[{"role": "user", "content": prompt}])
          return resp.content[0].text
        return await asyncio.to_thread(_call)

    Security note: api_key parameter comes from config; never log it. Add a __repr__ that excludes the client (returns "AnthropicLLM(model=...)").

    If the anthropic SDK raises anthropic.APIError, let it propagate — do not swallow errors.
  </action>
  <verify>
    <automated>cd /c/Users/HomePC/Documents/GitHub/memoryAgent && uv run --extra dev --extra cloud python -c "from mnema.adapters.llm.anthropic import AnthropicLLM; a = AnthropicLLM(api_key='test'); print('AnthropicLLM ok:', hasattr(a, 'complete'))"</automated>
  </verify>
  <done>AnthropicLLM class exists with constructor and async complete method; imports without error under cloud extra; LLM contract test with anthropic param passes when MNEMA_TEST_ANTHROPIC=1 and real API key is set</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QwenLLM adapter (PROV-03)</name>
  <files>src/mnema/adapters/llm/qwen.py</files>
  <behavior>
    - QwenLLM(api_key="test", default_model="qwen-flash") — constructor does not raise
    - async complete() returns a non-empty string when called with a real DashScope API key
    - QwenLLM satisfies LLMProvider Protocol shape
    - DashScope global state (dashscope.api_key) is set in __init__ (Pitfall 4 documented in docstring)
    - LLM conformance test with qwen param passes when MNEMA_TEST_DASHSCOPE=1 and DASHSCOPE_API_KEY set
  </behavior>
  <read_first>
    src/mnema/adapters/llm/stub.py — copy class shape exactly (same Protocol, same async complete signature)
    04-RESEARCH.md Pattern 2 (lines ~281-312) — DashScope Generation.call() shape + global-state pitfall
    04-PATTERNS.md QwenLLM section (lines ~83-112) — asyncio.to_thread pattern for dashscope
    04-RESEARCH.md Pitfall 4 (lines ~749-754) — global state warning
  </read_first>
  <action>
    Create src/mnema/adapters/llm/qwen.py:

    Module docstring: "QwenLLM — Alibaba DashScope Qwen LLM adapter. Satisfies LLMProvider Protocol by structural typing. Direct dashscope SDK (D4-05) — no LiteLLM. Sync SDK wrapped in asyncio.to_thread (D-13). Warning: dashscope uses module-level global state for api_key — use a single QwenLLM instance per engine config."

    Imports: from __future__ import annotations; import asyncio; (dashscope imported lazily inside __init__)

    Class QwenLLM (no inheritance):
      __init__(self, api_key: str, default_model: str = "qwen-flash") -> None:
        import dashscope  # noqa: PLC0415
        dashscope.api_key = api_key
        self._default_model = default_model
        self._dashscope = dashscope  # hold reference for use in complete()

      model property -> str: return self._default_model

      async complete(self, prompt: str, *, model: str | None = None) -> str:
        m = model or self._default_model
        dashscope = self._dashscope
        def _call() -> str:
          resp = dashscope.Generation.call(model=m, messages=[{"role": "user", "content": prompt}], result_format="message")
          if resp is None or resp.output is None:
            raise ValueError(f"QwenLLM: null response from DashScope (model={m!r})")
          return resp.output.choices[0].message.content
        return await asyncio.to_thread(_call)

    Note: DashScope Generation.call() shape is ASSUMED (A1 in RESEARCH.md). After `uv sync --extra cloud`, verify the call shape matches the installed dashscope version. If the call signature differs (e.g., TextGeneration vs Generation, or choices is at a different path), update accordingly. The asyncio.to_thread wrapper stays regardless.

    After writing the file, run the hermetic suite to confirm no regression.
  </action>
  <verify>
    <automated>cd /c/Users/HomePC/Documents/GitHub/memoryAgent && uv run --extra dev --extra cloud python -c "from mnema.adapters.llm.qwen import QwenLLM; q = QwenLLM(api_key='test'); print('QwenLLM ok:', hasattr(q, 'complete'))"</automated>
  </verify>
  <done>QwenLLM class exists with constructor and async complete method; imports without error under cloud extra; hermetic suite (56+ tests) still passes; LLM contract test with qwen param passes when MNEMA_TEST_DASHSCOPE=1 and real key set</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/env → AnthropicLLM/QwenLLM | API keys passed in via constructor; must never appear in logs, __repr__, or error messages |
| Cloud SDK response → LLMProvider.complete() return value | resp.content[0].text / resp.output.choices[0].message.content — validate non-null before return |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | AnthropicLLM api_key | mitigate | api_key stored only in self._client (not self._api_key); __repr__ excludes client; never logged |
| T-04-02-02 | Information Disclosure | QwenLLM api_key via dashscope global | mitigate | dashscope.api_key is module-level state; single-engine config means one key in process; document global-state risk in docstring (Pitfall 4) |
| T-04-02-03 | Denial of Service | asyncio.to_thread blocking | accept | Sync SDK calls block a thread pool thread, not the event loop; acceptable per D-13 for v1; mark for AsyncAnthropic/AsyncClient migration in v2 |
| T-04-02-04 | Tampering | prompt injection via complete() | accept | LLMProvider is a pass-through; input validation is ConsolidationPipeline's responsibility; adapter is transparent |
| T-04-02-05 | Repudiation | API call not logged | accept | Audit logging for LLM calls is a v2 concern; Phase 4 does not add call logging |
</threat_model>

<verification>
1. `cd /c/Users/HomePC/Documents/GitHub/memoryAgent && uv run --extra dev pytest tests/ -q 2>&1 | tail -5` — full hermetic suite still passes
2. `uv run --extra dev --extra cloud python -c "from mnema.adapters.llm.anthropic import AnthropicLLM; from mnema.adapters.llm.qwen import QwenLLM; print('both import ok')"` — no ImportError
3. `uv run --extra dev pytest tests/conformance/test_llm_contract.py -q -v 2>&1 | tail -10` — stub param PASSED; anthropic/qwen params SKIPPED (no env vars in CI)
4. `uv run --extra dev pyright 2>&1 | tail -3` — 0 errors (adapter files are in src/mnema, covered by pyright)
</verification>

<success_criteria>
- AnthropicLLM in src/mnema/adapters/llm/anthropic.py: no Protocol inheritance, asyncio.to_thread around Anthropic.messages.create(), api_key in constructor not hardcoded, __repr__ excludes key
- QwenLLM in src/mnema/adapters/llm/qwen.py: dashscope.api_key global state set in __init__, asyncio.to_thread around Generation.call(), null-response guard raises ValueError
- Both satisfy LLMProvider Protocol by structural typing (pyright verifies)
- Conformance llm_backend fixture anthropic/qwen params skip without env vars; pass when env + real keys provided
- Hermetic suite (all existing tests) still passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-cloud-providers-backends/04-02-SUMMARY.md`
</output>
